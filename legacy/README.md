# legacy/

Cópias antigas mantidas só até a nova base reproduzir todos os fluxos. **Não editar.**

- `index_teste.html` — o app inteiro sem Firebase, como era antes da migração. Foi
  substituído por `npm run dev:teste` (modo memória, ver `src/infrastructure/`).
  Ainda abre direto do disco: os sprites apontam pra `../public/idle/`.

Quando o `npm run dev:teste` estiver validado nos fluxos principais, esta pasta some.
