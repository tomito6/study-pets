# Study Pets

## Como usar este documento

Este arquivo é um **guia vivo**, não uma especificação imutável. Ele descreve a estrutura e as intenções do projeto, mas **o código é sempre a fonte da verdade**. Valores numéricos, listas e detalhes mudam — se você notar divergência entre o que está aqui e o que o código diz, confie no código e atualize este arquivo.

Quando o usuário pedir mudanças que conflitam com algo aqui, **assuma que ele está atualizando o design**, não violando uma regra. Sugira atualizar este arquivo no fim.

## Sobre o projeto

### A ideia central

O **Study Pets** é um app de Pomodoro que trata estudar como uma jornada — não uma punição. Nasceu da frustração com planos rígidos: o Tomi começou com uma planilha de Excel cheia de blocos fixos, percebeu que a vida real (almoço atrasado, aula no meio da manhã, cansaço) destruía o plano todo dia, e foi construindo algo que se adapta em vez de quebrar.

A evolução foi: planilha → app web simples → app web flexível → app com login na nuvem → RPG. Cada passo veio de uma dor real, não de querer features. Manter esse espírito é importante — features novas devem resolver problema de verdade, não inflar.

### Quem usa

Hoje só o Tomi, brasileiro estudando na TUM (Munique). A ambição é abrir pra qualquer estudante no mundo no futuro. Isso significa duas coisas: o app precisa funcionar pra alguém que tem rotina caótica de universitário (eventos no meio do dia, horários variáveis, dias bons e dias ruins), mas as decisões de design não devem ser tão pessoais que não se generalizem.

### A vibe

Sério e produtivo na base. O Tomi não quer um app que pareça infantil — ele estuda de verdade, tem objetivos concretos (TUM, agosto 2026), e o app precisa parecer uma ferramenta legítima de produtividade. Tema escuro, tipografia limpa, dados claros, sem decoração que distrai.

Mas em cima dessa base entra calor: pets de pixel art, animação de personagem, sistema de XP, sons agradáveis quando completa um bloco. O objetivo é dar **companhia e reconhecimento** — não estudar sozinho, e ver que o esforço gera evolução visível. A gamificação serve a essa função emocional, não vice-versa.

Os pets têm um papel especial nesse senso de progresso: são marcos tangíveis do esforço. Cada pet adotado é prova concreta de horas estudadas — diferente de um número de XP abstrato, é algo que você conquistou e que fica ali, com personalidade, te acompanhando. Conforme a coleção cresce, a evolução fica visível de um jeito que dados não conseguem capturar.

### Os dois motivos pra voltar

1. **Disciplina/hábito**: o app facilita voltar todo dia, sem fricção. Plano se adapta ao que você consegue fazer, não ao que o app esperava de você.
2. **Estudo gostoso**: o ato de marcar um bloco como feito tem que ser gratificante. Pequeno som, pequena animação, XP somando, moeda ganha, talvez um pet acompanhando. O reforço positivo vem do app, não da pressão.

### O que evitar

- **Padrões manipulativos**: nada de "você está perdendo sua sequência!" como ameaça. Streaks são pra celebrar, não pra causar ansiedade.
- **Infantilização**: pets e níveis sim, mas com pixel art bonito e linguagem adulta. Não é um app pra criança aprender a estudar.
- **Rigidez**: o app NUNCA deve fazer o usuário se sentir mal por mudar o plano. Almoçou cedo? Tudo bem, ajusta. Faltou um dia? Tudo bem, recomeça. A vida não para por causa do Pomodoro.
- **Inflar features**: se uma feature não resolve dor real, não merece estar aqui. O app já tem muita coisa.

### Tom de comunicação

Falar português brasileiro com o usuário. Direto, com leveza, sem formalidade desnecessária. O Tomi gosta de honestidade — se algo é má ideia, diga; se algo não é problema, não invente. Evitar bullet points em respostas conversacionais (eles cabem em documentação).

## Stack & deploy

- Frontend: React + TypeScript (Vite). Uma árvore React só (`src/app/App.tsx`); o app vanilla de arquivo único foi migrado por completo em 2026-09 (história em `plans/2026-09-02_1552_migracao-vite-ts.md`)
- Build/testes: Vite + TypeScript + Vitest. `vercel.json` declara build e output explicitamente — não depender da detecção automática da Vercel
- PWA: `vite-plugin-pwa` gera o service worker (workbox) **só no build** — precache do `dist`, e a navegação sai do próprio precache (`navigateFallback` no `index.html`, `runtimeCaching` vazio): abre offline, mas não tenta a rede antes. Quem faz o deploy novo chegar é o `registerType: 'autoUpdate'` — o SW novo assume assim que instala, e o reload seguinte já serve o precache novo. Em dev/teste nada é registrado (HMR e e2e intactos). Manifest estático em `public/manifest.webmanifest`, ícones em `public/icons/` (gerados do personagem por `scripts/app-icon.mjs`). Se o browser prender uma versão velha: DevTools → Application → Service Workers → Unregister
- Backend: Firebase (Auth via Google e e-mail/senha + Firestore, com cache persistente em IndexedDB — sem rede, `load` volta do cache e o save entra na fila)
- Hosting: Vercel (deploy automático no push pra `main`)
- Repo: github.com/tomito6/study-pets
- URL: plano-estudos-one.vercel.app — o projeto no Vercel ainda se chama `plano-estudos`, e é o nome do projeto que gera essa URL. **Não renomear o projeto**: mudaria o domínio, que teria de ser re-autorizado no Firebase Auth (Authorized domains) pro login com Google continuar funcionando.

## Arquivos

- `index.html` — `<div id="root">`, um `preload` da fonte da primeira pintura e as tags do PWA (manifest, ícone, apple-touch-icon, theme-color). Nada mais. **As fontes saíram do CDN do Google em 2026-09-11** e são servidas pelo próprio domínio (`public/fonts/`, declaradas em `src/styles/fonts.css`, importado antes do `app.css` no `main.tsx`): o `<link>` antigo mandava o IP de todo visitante pro Google antes de qualquer tela, e o app é publicado da Alemanha (LG München I, 20.01.2022, 3 O 17493/20). Pesos que dividem os mesmos bytes apontam pro MESMO arquivo — DM Sans e Lora são variáveis, e um nome por peso fazia o navegador baixar 37 KB idênticos três vezes
- `src/main.tsx` — entry: monta `<ErrorBoundary><App/></ErrorBoundary>` e chama `startSession()` (auth → carregar doc → boot)
- `src/app/` — `App.tsx` (a árvore inteira: login, `#app` com cabeçalho, timer, abas, modais), `Header.tsx` (abas — sem emoji desde 2026-09-06 —, data, XP, o **sininho** das notificações e Sair. **No celular a barra é empilhada** (`.topbar-stacked`, 2026-09-11): a data e o estado (XP · sininho · Sair) numa faixa, e as **abas numa faixa própria** embaixo, num trilho, ocupando a largura inteira (alvos de 114×31 em vez de três pílulas espremidas num canto). Antes tudo disputava uma linha só e a altura da barra mudava conforme o número de XP crescia — 76px numa conta nova, 109px numa de cinco dígitos; agora é fixa em qualquer largura e com qualquer número, e o e2e 50 cobra isso. O **nome do nível** no selo de XP (`#top-level`) só existe a partir de 1100px, pelo mesmo motivo; o nível continua no Perfil e na Análise; a altura **medida** vai em `--topbar-h` no `#app` (`app/useTopbarHeight.ts`, `ResizeObserver`), que a barra do timer usa pra grudar logo abaixo em vez de um `top` fixo — era um `76px` cravado no CSS até 2026-09-11, e numa tela estreita, onde a barra quebra em duas linhas, a `.timer-bar` sumia debaixo dela. Em tela grande o mesmo elemento vira a **barra do topo** do layout B7 (`.topbar.topbar-wide`, atravessando as duas colunas): a marca à esquerda, as abas no meio e, à direita, o XP/nível, a engrenagem `#gear-btn` e o avatar `#avatar-btn`, que abre o menu `#avatar-menu` com Configurações e Sair (`#avatar-sair`) — ver "Tela grande") e `ErrorBoundary.tsx` (erro de render vira a tela "Algo quebrou" com Recarregar, em vez de tela preta). Quem mostra/esconde `#app` e a aba ativa é o `App`, lendo o store
- `src/features/<feature>/` — UI React por feature:
  - `auth/LoginScreen.tsx` — formulário de e-mail/senha (Entrar / Criar conta / Esqueci a senha, erro inline, estado de carregando) com o botão do Google abaixo, separado por "ou". Ids: `#login-email`, `#login-password`, `#login-submit`, `#login-toggle-mode`, `#login-forgot`, `#login-error`, `#login-reset-sent`
  - `plan/` — a aba Plano inteira: `PlanTab.tsx` (XP card, stats do dia, semana/dia, "Encerrar o dia"), `BlockList.tsx` (ciclos e linhas com check — a lógica de clique do `renderBlocks` antigo mora aqui), `DayWindowsPanel.tsx` (o modal "Janelas do dia": editor de janelas só deste dia, "Começar agora", "Dia livre", "Restaurar rotina"), `feedback.ts` (ripple + "+X XP" flutuante + a faixa `#cycle-cheer` da leva fechada), `useMinuteTick.ts` (re-render na virada do minuto, pro destaque "agora"), `PlanSidebar.tsx` (a coluna da **esquerda** da tela grande: XP pendente de hoje, a meta diária com a barra e "N de 7 dias" — sem dots — e o quarto ilustrado `RoomCard` (`#room-card`), com o personagem e o pet equipado), `useColumnFit.ts` (mede a coluna e põe `--col-k` no `#app` pra ela caber na altura da janela por escala, nunca espremendo card) , `EventDragGhost.tsx` (`#drag-ghost`: onde o evento arrastado cai, desenhado na lista) e `WeekView.tsx` (a Semana da tela grande: um dia por coluna, blocos na altura da duração — lê `blocksForDay`, não gera nada; evento se arrasta dali pra outro horário ou outro dia)
  - `timer/` — `TimerBar.tsx` (barra "Em andamento" / "Começa em" / "Pausado · N", volume, ⏸ Pausar ↔ ▶ Retomar (`#timer-pause`), ✕ Parar) e `FocusOverlay.tsx` (modo foco: anel que drena, próximo bloco, ganho ao concluir; contagem até o início quando aberto antes da hora; faixa "✓ … concluído" ao emendar no bloco seguinte; o botão `#focus-pause` abaixo do "Em seguida" — ver "Pausar o bloco"). Os dois **derivam** o restante do relógio a cada segundo com `useSecondTick` — o store só sabe qual bloco está rodando (e desde quando está pausado, `derived.timerPausedAt`). `HardcoreModals.tsx` = o consentimento antes de entrar num bloco com o modo hardcore ligado (`#hardcore-start-confirm`) e a confirmação de desistir dentro do foco (`#hardcore-quit-confirm`, filha do overlay — que é um stacking context acima dos modais). `SiteBlockBadge.tsx` = a faixa "🛡️ N sites bloqueados" que os dois mostram enquanto a extensão confirma (`#timer-site-block` na barra, `#focus-site-block` no foco)
  - `events/` — os dois modais de evento do Plano: `EventPanel.tsx` (novo evento, com os **atalhos** 🍽️ Refeição / 👥 Reunião / ☕ Café / 🚌 Deslocamento / ✏️ Outro no topo — só preenchem o formulário, catálogo em `domain/eventPresets.ts` — e recorrência; com a prop `edit` vira "Editar evento", preenchido, e Salvar substitui o avulso — numa série, o chip "Aplicar a" escolhe **só este dia** (exceção + avulso, `updateSeriesOccurrence`) ou toda a série), `EventDeleteModal.tsx` (Editar / só este dia / a série / avulso) e `EventMoveModal.tsx` (`#event-move-scope`: a mesma pergunta depois de **arrastar** uma ocorrência de série). São **filhos do `PlanTab`**, que guarda qual está aberto em `useState` — modal é estado local, não vai pro store. Aqui também mora `useEventDrag.ts`, a máquina de ponteiro de arrastar evento, usada pelo Dia e pela Semana (ver "Arrastar eventos")
  - `groups/` — grupos de estudo: `useGroupSelection.ts` (a máquina de estados da seleção de linhas: botão direito, toque longo, botão "Agrupar"), `GroupPanel.tsx` (novo/editar grupo), `GroupBox.tsx` (a caixa na lista) e `SelectionRect.tsx` (o retângulo tracejado em volta das linhas selecionadas). A seleção é `useState` do `PlanTab`; o modal também
  - `analytics/AnalyticsTab.tsx` — a aba Análise inteira (cartão de perfil + sparkline, sub-nav Hoje/Semana/Geral/Recordes como estado local, aderência, dots da meta, heatmap, horas, drop-off, recordes). Só formata: os cálculos estão em `domain/analytics.ts`
  - `profile/ProfileTab.tsx` — a aba Perfil (hero com personagem e pet animados por `useSpriteFrame`, stats, card do pet ativo, botões da loja e de "Meus pets"). Roda `applyPendingPetXP` ao ficar visível
  - `pets/` — `PetCard.tsx` (`ShopPetCard` = espécie à venda; `OwnedPetCard` = pet adotado, com nome, forma, Equipar, Evoluir e skills), `PetModals.tsx` (loja, meus pets, o modal de um pet só `#pet-detail-panel`, adoção com nome, renomear, escolher caminho de evolução — filhos do ProfileTab) e `NameField.tsx` (o campo de nome com sugestão + 🎲, usado em adotar, renomear e no pet inicial)
  - `notifications/NotificationBell.tsx` — o **sininho** e o painel (ver "Notificações"). Popover ancorado, não modal; fecha clicando fora ou com Esc (`shared/useDismiss.ts`, extraído do menu do avatar). Ids: `#notif-btn`, `#notif-badge`, `#notif-panel`, `#notif-list`, `#notif-empty`, `#notif-clear`
  - `settings/` — a página de Configurações inteira: `SettingsPage.tsx` (Geral / Estrutura do dia — nessa ordem, e abre no Geral; o botão ⚙️ flutuante e "aberta ou fechada" como estado local; Geral tem o card "Meus dados" com "Baixar (JSON)"), `ConfigPreview.tsx` ("Como fica o dia": a linha do tempo da rotina + tiles), `DayTimeline.tsx` (a régua de horas com um segmento por bloco; geometria em `domain/timeline.ts`), `StudyWindowsEditor.tsx` (também usado pelo modal "Janelas do dia"), `FitStudyModal.tsx` (Encaixar estudo — simula com as janelas do dia visível, editadas ou não), `SiteBlockSection.tsx` (a seção "Bloqueio de sites" do Geral: o switch, os chips de modo, a caixa de sites, os chips do que foi entendido e o que não foi, os três estados do status da extensão e o "▶ Testar por 1 min" — ver "Bloqueio de sites"), `CalendarImportSection.tsx` (a seção "Importar calendário" do Geral e a tela de revisão `#ics-review-panel` — ver "Importar calendário"), `DangerModals.tsx` (cancelar sessão, apagar conta). O formulário é um rascunho (`ConfigDraft`) que só vira config ao Salvar
  - `avatar/AvatarControls.tsx` — os controles do personagem (preview animado + pele, cabelo, penteado, corpo), **controlados**: as Configurações mandam a escolha direto pro store (`setAvatar` salva na hora) e o onboarding guarda num `useState` até o fim
  - `onboarding/OnboardingModal.tsx` — "Bem-vindo!" em três passos: o personagem, o pet inicial (os dois só quando não há pet) e período de uso + fins de semana. Aberto pelo boot (conta nova) e por cancelar sessão
  - `dayend/DayEndModals.tsx` — confirmação de encerrar o dia, o resumo com os ganhos, e o prompt automático "passou do horário" (encerrar / prolongar)
  - `tutorial/TourBalloon.tsx` — o balão do tour contextual (um por vez, ancorado no elemento real da aba; cartão fixo no rodapé se o elemento não existe). Qual área está com o tour vem do store (`activeTourArea`); qual balão da área está aberto é `useState` daqui
  - `shell/Modal.tsx` — a casca `.panel-overlay.center > .panel-sheet` de todo modal; clicar fora fecha
  - `shell/SaveIndicator.tsx` — "Salvando… / Salvo ✓"
- `src/store/store.ts` — estado central tipado, um objeto só mutado no lugar pelos casos de uso; quem muta chama `notify()`; React lê com `useAppState`. `derived` = runtime não persistido: `weeks` (calculado por `rebuildWeeks`), `timerBlock` + `focusOpen` + `timerCompleted` + `audio` (`application/timer`), `save` (status), `authReady`, `onboardingOpen`, `dayEnd` (modais do fim do dia), `hardcore` (a sessão), `siteBlock` (o ack da extensão + o teste de 1 min) e os dois **pedidos** — `startRequest` (o "Iniciar" do cartão Agora da tela grande; o `PlanTab` atende e limpa) e `settingsRequest` (`{ focus }`: a engrenagem e o menu do avatar da barra mandam `null`, o "+ Evento" manda `'calendar'`; a `SettingsPage` atende, rola até a seção e limpa). Modal aberto por **clique local** fica em `useState` do componente; só entra no store o que um **caso de uso** abre
- `src/application/` — casos de uso e leituras do estado. UI chama isto; isto chama domínio/infra:
  - `plan.ts` — `rebuildWeeks`, `blocksForDay` (com a memoização do gerador; usa a config **do dia**: janelas editadas — a refeição chega como evento, via `getEventsForDate`), `allDays` (os dias que contam — fim de semana pausado e dia livre ficam de fora; um fim de semana com janelas abertas entra, marcado como **dia bônus** — `restKindOf`/`isBonusDayKey` dizem o que cada dia é), `computeStatsNow` (memoizado por versão do store: cabeçalho, Plano, Análise e Perfil pagam uma passada só), `dateForWeekDay`/`findWeek`/`forEachDay`
  - `checks.ts` — `toggleBlockCheck` (marca/desmarca, grava o **id da instância** equipada e o bônus da skill dela via `bonusForCheck`, devolve XP/moedas pro feedback) e `checkBlock` (marca só se ainda não está marcado — o fim do bloco no modo foco usa; nunca desmarca)
  - `save.ts` — `scheduleSave` com debounce, `blockSaves` (apagar conta), status pro indicador, `CLIENT_ID` (identifica esta carga da página; vai no `meta.writer` de cada save) e `hasPendingSave()`
  - `sync.ts` — sync entre dispositivos: `subscribeRemote`/`unsubscribeRemote` (a inscrição no snapshot do doc) e `applyRemoteDoc` (reidrata o estado com o doc que chegou do servidor — a menos que seja eco da própria escrita, a mesma emissão de novo, o onboarding esteja aberto ou haja save local pendente; nunca toca `derived`)
  - `timer.ts` — `tryStartTimer` (valida: só bloco de hoje que ainda não terminou — antes da hora fica em espera; devolve o motivo pra UI mostrar o toast), `startTimer`/`stopTimer`/`closeFocus`, `requestStartBlock`/`clearStartRequest` (o "Iniciar" pedido de fora da lista — o cartão Agora do laptop: quem atende é o `PlanTab`, dono do consentimento do hardcore, então o cartão nunca fura o hardcore), `reconcileTimer` (acerta o timer com o relógio: o fim do bloco pelo mesmo caminho, um bloco por vez se vários passaram — é o que o `setInterval` chama a cada segundo **e** o que roda ao voltar pra aba/destravar o celular, via `watchVisibility`), o Wake Lock enquanto o foco está aberto, o fim do bloco (**no foco** → `checkBlock` + som `sucesso` + emenda no bloco seguinte via `chainedBlockAfter`; **fora do foco** → som do tipo + notificação e o timer some), e o áudio (`playSound`, `toggleMute`, `setVolume`). **Não há `notify()` por segundo** — isso faria o app inteiro re-renderizar e recalcular stats
  - `hardcore.ts` — o modo hardcore: `startHardcore` (mesmas recusas do timer + bloco abandonado), `quitHardcore` ("Desistir": cobra; "Parar aqui" na pausa e "Cancelar" em espera: de graça), `hardcoreQuitPreview` (a conta pro modal) e `resumeHardcoreOnBoot` (a sessão que ficou no dispositivo: volta pro foco, ou cobra o abandono). `hardcoreRuntime.ts` — a sessão em `derived.hardcore`: `armHardcoreIfRunning` (o bloco começou a rodar → persiste e arma o guard de fechar a aba), `hardcoreChained`/`endHardcoreSession` (o timer chama). Dois arquivos porque o timer precisa do segundo e o primeiro precisa do timer. **Nenhum dos dois fala com a extensão** desde 2026-09-08
  - `pause.ts` — pausar o bloco em andamento: `pauseTimer` (só rodando, fora do hardcore, dia aberto — devolve o motivo), `resumeTimer` (a pausa vira `PauseRecord` do dia, o plano é regenerado, checks e grupos acompanham os blocos que deslizaram, o timer segue no bloco regenerado — ou para sem check se a pausa atravessou o fim), `resumePauseOnBoot` (a pausa que ficou aberta no dispositivo: o timer volta pausado na barra) e `timerPaused`. Os ganchos do runtime (`pauseRuntime`/`resumeRuntime`/`adoptPausedBlock`, e o `reconcileTimer` que não termina bloco pausado) ficam em `timer.ts` — o mesmo arranjo do hardcore, sem ciclo. Ver "Pausar o bloco"
  - `siteBlock.ts` — o bloqueio de sites, fonte única do que a extensão recebe: `syncBlocking` (idempotente, o timer chama a cada acerto de relógio; estudo **pausado** continua bloqueando, com o `until` deslizando ao minuto), `stopBlocking` (o app encerrou), `answerExtensionQuery` (responde `unknown` sem estudo rodando — recarregar não libera), `startSiteBlockTest`/`stopSiteBlockTest` (o "Testar por 1 min"), `blockingNow` (o ack da extensão) e `watchExtension`
  - `events.ts` — `addEvent`/`addEventSeries`, `updateEvent`/`updateSeries`/`findEventEditTarget`, `deleteEvent`/`deleteSeriesOccurrence`/`deleteSeries`, `updateSeriesOccurrence` (editar só um dia da série: exceção + avulso), `moveEvent` (o arrasto: horário novo, e na Semana dia novo; `canMoveEvents`/`isMovableBlock`/`moveNeedsScope`), validações com motivo. Cada mutação limpa o cache do gerador, salva, notifica e emite o toast "Plano reajustado: …" (`notifyPlanDelta`, que `settings.ts` e `dayWindows.ts` também usam)
  - `calendarImport.ts` — importar calendário: `loadIcsFile` (lê o arquivo pela infra, calcula o horizonte — de hoje até o `periodEnd` ou um ano — e chama `readIcs`), `applyIcsImport` (só o que foi marcado; apaga antes o que tem o mesmo carimbo de origem, então reimportar substitui em vez de duplicar) e `countChosen`
  - `dayWindows.ts` — as janelas só de um dia: `setDayWindows`, `startNow` ("Começar agora", só hoje), `setDayOff` (dia livre — só hoje/futuro, e hoje só sem check), `clearDayWindows` ("Restaurar rotina"), `canEditDayWindows` (encerrado e passado são read-only), `effectiveWindows` (as janelas que valem: editadas → nenhuma no fim de semana pausado → rotina)/`dayWindowsOverride`/`restKindKey`/`isRestDayKey`. Cada mutação também refaz as semanas e reagenda o prompt de fim de dia
  - `export.ts` — "Baixar meus dados": `buildExport` (o documento salvo + `exportedAt`, sem uid/e-mail/meta **e sem `safetyNet`**) e `exportMyData` (dispara o download pela infra)
  - `backup.ts` — a rede de segurança e o caminho de volta: `takeSafetyNet` (a foto de antes de apagar, que `cancelSession` tira), `restoreSafetyNet`/`discardSafetyNet`/`activeSafetyNet`, `dropExpiredSafetyNet` (o boot descarta a vencida) e `importBackupFile` (um JSON baixado volta a ser o estado). Tudo que restaura passa pelo mesmo `aplicar`, que usa `hydrateUserDoc` — então arquivo de meses atrás entra igual
  - `settings.ts` — `saveSettings` (preserva `periodStart`, refaz semanas, reagenda o prompt de fim de dia; **recusa campo numérico vazio**, que antes virava `NaN` salvo), `cancelSession` e `requestSettings`/`clearSettingsRequest` (a barra do laptop pede pra abrir as Configurações; a `SettingsPage`, dona do "aberta ou fechada", atende e limpa)
  - `account.ts` — `deleteAccount`: doc primeiro, usuário depois; devolve o estágio que falhou
  - `dayRollover.ts` — a virada da meia-noite com o app aberto: `startDayRollover`/`stopDayRollover` chamam `applyPendingPetXP` no instante da virada (um `setTimeout`, mais o `onVisible` porque aba em segundo plano tem o timer estrangulado)
  - `notifications.ts` — o sininho: `pushNotifications` (idempotente por id, salva e notifica só se algo entrou), `markNotificationsRead` (ao FECHAR o painel), `clearNotifications`, `notifications`/`unreadNotifications`. Quem **emite** é o caso de uso dono do acontecimento — `dayEnd` (o fim do dia), `pets` (`applyPendingPetXP`, inclusive no boot) e `hardcore` (o abandono)
  - `pets.ts` — `applyPendingPetXP` (idempotente), `coinBalance`, `buyPet` (espécie + nome, com motivo de recusa), `adoptStarter`/`needsStarter` (o pet inicial, grátis, só sem pet nenhum), `toggleEquip`, `toggleSkill`, `renamePet`, `evolvePet`, `activePet`/`petById`
  - `dayEnd.ts` — `closeDay` (trava checks, credita pets, monta o resumo — com as pausas do dia; para um timer pausado), o prompt automático agendado pro fim do último estudo (sem polling; não abre enquanto o timer está pausado — retomar reagenda), `extendDay` (num dia com janelas editadas, estica o override; senão a rotina)
  - `avatar.ts` — a aparência do personagem: `currentAvatarSprites` (os 4 frames prontos), `avatarSpritesOf` (os frames de uma aparência qualquer — o onboarding mostra a escolha antes de aplicá-la) e `setAvatar` (troca, salva e notifica; devolve `false` se nada mudaria)
  - `onboarding.ts` — `finishOnboarding` (período + pet inicial; valida tudo com motivo antes de mudar qualquer coisa)
  - `tutorial.ts` — o tour contextual: `currentTourArea` (leitura), `finishTour` (área vista — "Entendi" no último balão e "Pular"), `restartTour` ("Ver o tour de novo": zera `tutorialSeen`). Avançar entre balões não persiste nada e fica no componente
  - `groups.ts` — `addGroup`/`updateGroup`/`deleteGroup` (com motivo de recusa), `validateGroup`, `canEditGroups`. Grupo não mexe no plano: só salva e notifica, sem `clearBlockCache`
  - `session.ts` — entrar/sair e o boot: `startSession` registra o listener de auth (e o `watchVisibility` do timer); a cada login, `loadUserData` → `initAfterLoad` (XP pendente, prompt, semana/dia visíveis, a sessão hardcore e depois a pausa que ficaram no dispositivo) → `subscribeRemote`; conta nova abre o onboarding; sair cancela a inscrição. Também `signUpWithEmail`/`signInWithEmail`/`resetPassword`: validam com `domain/auth.ts` antes de chamar a porta e devolvem `{ok:true} | {ok:false,reason}` (mesmo padrão dos outros casos de uso) — `resetPassword` sempre resolve `ok:true` com e-mail de formato válido, mesmo se a conta não existir, pra não revelar contas cadastradas
- `src/domain/daySummary.ts` (o resumo do dia entre dois instantâneos, mais as pausas do dia se houve) e `src/domain/endOfDay.ts` (último estudo, quando perguntar, prolongar — `extendWindowsTo`/`extendDayTo`)
- `src/domain/dayWindows.ts` — janelas só de um dia: `configForDay` (a config efetiva do dia), `isDayOff`, `restDayKind`/`isBonusDay`/`windowsForDay` (o que é dia de descanso — fim de semana pausado ou dia livre — e o dia bônus: sábado pausado em que o usuário abriu janelas), `validateDayWindows` (fim > início, sem sobreposição), `startNowWindows` (a janela que contém agora, ou a próxima, passa a começar no próximo múltiplo de 5 min)
- `src/domain/pets.ts` — o catálogo: `FORMS` (o que aparece na tela: sprite + skills) e `PETS` (espécies à venda, com caminhos de evolução). Adicionar pet = uma forma + uma espécie aqui + sprites em `public/idle/pets/{form}/`. Também a curva de nível própria do pet, nome (`normalizePetName`/`suggestPetName`), instância (`newPetInstance`/`legacyPetInstance`), forma atual (`petForm`) e evolução (`evolutionOf`/`evolve`), saldo de moedas
- `src/domain/analytics.ts` — `currentWeekKeys`, `adherence` (o card "Realizado": cumprido contra a **meta** do período — ver "Aba Análise"), `goalWeek` (os 7 dots; `restKind` diz por que um dia é neutro: `weekend` = fim de semana pausado, `off` = dia livre; um sábado com janelas abertas é dia normal), `heatmap` (7×16 células, intensidade por % da meta; células `weekend-off`/`day-off`), `hourBars`, `dropoff`, `sparkline` (8 semanas), `nextLevel`. `goalWeek` e `heatmap` recebem `startedAt` (= `config.periodStart`): dia anterior ao início da conta é **neutro** (`before`), nem falha nem descanso — sem isso, quem criava a conta numa quinta via segunda, terça e quarta reprovadas e 16 semanas de heatmap zerado
- `src/domain/settings.ts` — `ConfigDraft` ↔ `UserConfig` (`draftFromConfig`/`normalizeConfig`), `summarizeConfig` (o Resumo do dia), `fitStudySuggestions` (o algoritmo do Encaixar), formatação de durações
- `src/domain/planDelta.ts` — o que mudou no plano de um dia (estudos a mais/menos, novo fim, o último estudo que mudou de tamanho), puro; `planDeltaParts` são os pedaços da frase, que o toast da pausa reaproveita
- `src/domain/ics.ts` — a leitura de calendário (.ics), pura: `readIcs(texto, {from, to})` devolve o **plano de importação** (o que vira série, o que vira lista de datas, o que ficou de fora com o motivo) e `itemOccurrences`. Desdobra linha continuada, resolve `TZID`/UTC pro relógio local, entende `RRULE` (inclusive `BYDAY` posicional como `3TH`), `EXDATE` e `RECURRENCE-ID` — ver "Importar calendário"
- `src/domain/eventDrag.ts` — arrastar evento, puro: `minuteAtY`/`yAtMinute` (posição na tela ↔ minuto do dia, interpolando dentro da faixa — serve a lista do Dia, que não é proporcional, e a coluna da Semana, que é), `fieldAtX` (o dia sob o ponteiro), `grabOffsetMin` (o ponto de pega), `movedRange` (duração preservada, passo de 5 min, sem vazar do dia)
- `src/domain/eventPresets.ts` — os atalhos do "Novo evento" (`EVENT_PRESETS`: refeição, reunião, café, deslocamento — nome com ícone, horário típico, duração, sem XP, repete todo dia ou não), `startsWithEmoji` (evento com ícone próprio não ganha 📅 no plano), `mealSeries` (a refeição de todo dia: série semanal em todos os dias, sem XP, sem âncora, id `ser_almoco`) e `migrateLunch` (o almoço da config antiga → série + exceções/avulsos)
- `src/domain/cycles.ts` — a leva como unidade que fecha: `cycleSummary` (feitos/total, minutos, XP com o bônus do check, moedas), `closedCycleOf` (a leva deste bloco fechou e vale comemorar?) e `MIN_BLOCKS_TO_CHEER`. Puro — ver "O ciclo fecha"
- `src/domain/timeline.ts` — `timelineOf` (a régua de horas de um plano: da hora cheia antes do primeiro bloco à hora cheia depois do último, mínimo 4h; um segmento em % por bloco, marcas de hora em passo inteiro) e `timelinePosition` (onde o "agora" cai). Quem desenha é `features/settings/DayTimeline.tsx`
- `src/domain/tutorial.ts` — o tour contextual: `TOUR_STEPS` (os cinco balões: id, área, âncora = seletor do elemento real, lado e alinhamento), `activeTourArea` (qual área mostra o tour: aba visível, ainda não vista, sem onboarding), `nextTourStep`, `markTourSeen`, `normalizeTutorialSeen` (leitura do doc), `placeBalloon` (a geometria **nos dois eixos**: recebe a faixa visível do documento — `TourViewport`, com o topo já descontado da barra fixa — e devolve onde o balão cabe; puro, testado pra nunca sair da tela nem ficar atrás da barra) e `scrollToShow` (precisa rolar a página até o elemento? o `scrollY` alvo, ou null). Os textos ficam em `strings.tutorial.steps`, indexados pelo id do passo
- `src/domain/siteBlock.ts` — o bloqueio de sites, puro: `normalizeSite`/`normalizeSites`/`parseSites` (o que virou domínio e o que **não** virou), `SITE_ALIASES`/`aliasesOf`/`expandSites` (os apelidos), `normalizeSiteBlockConfig`/`siteBlockArmable` e `migrateSiteBlock` (a lista que morava no hardcore, v3 → v4)
- `src/domain/hardcore.ts` — o modo hardcore, puro: `normalizeHardcoreConfig` (só `{ enabled }`), `HardcoreSession` + `sessionFor`/`parseHardcoreSession`/`resolveSession` (a sessão no dispositivo: `resume` / `abandon` / `expired-free`), `penaltyFor` (2× o XP do estudo; pausa 0), `quitCost` (a conta de desistir agora: limitada ao saldo, com nível antes/depois de usuário e pet), `isForfeited`/`penaltyRecord`/`normalizePenalties`
- `src/domain/pauses.ts` — pausar o bloco, puro: `pauseRecordFor` (o registro: começo pra baixo, duração pra cima, mínimo 1), `normalizePauses`/`addPause`/`pausesTotal`, a pausa aberta no dispositivo (`pauseSessionFor`/`parsePauseSession`), e a correspondência antes ↔ depois (`pauseRemap`: a cadeia contígua a partir do bloco pausado, um a um pela posição; `remapChecksForPause`/`remapGroupsForPause`) — ver "Pausar o bloco"
- `src/domain/notifications.ts` — o sininho, puro: `Notification` (`{id, kind, at, read, data}`), `addNotifications` (dedup por id, teto de `MAX_NOTIFICATIONS` = 40, mais nova primeiro — e **a linha antiga vence**, com o `read` e o carimbo dela), `unreadCount`/`markAllRead`, `normalizeNotifications` (leitura do doc) e `ageOf` (tempo relativo em pedaços; quem escreve a frase é `strings.notifications`)
- `src/domain/progressNotices.ts` — de acontecimento a notificação, puro: `petNotices` (o diff de nível/evolução antes ↔ depois de creditar), `creditedDaysNotices` (o que um ou mais dias que entraram na conta deixam: a linha do dia, o nível, o marco de sequência, o recorde e o cruzamento do saldo — com `MAX_DIAS_NO_LOTE`), `isStreakMilestone` e `abandonNotice`
- `src/domain/timer.ts` — `timerProgress` (restante = fim − agora, nunca um contador em memória — por isso aba suspensa e tela travada não atrasam o relógio; `phase` = `waiting`/`running`/`paused`/`done`, com a contagem até o início; `pausedAt` congela o restante e o bloco não termina; o total é `blockMins`, sem o tempo já pausado, então o anel drena sobre os minutos de estudo). Qual bloco está rodando, porém, é runtime (`derived.timerBlock`, não persistido): **um reload perde o timer**; só a sessão do hardcore volta, pelo `localStorage` — ver "Modo hardcore", `canStartBlock` (recebe um `StartContext` **obrigatório** — `{ closed, forfeited }`: enquanto era opcional, o "Iniciar" do cartão Agora furava a trava do dia encerrado), `soundForBlock`, `nextBlockAfter`, `chainedBlockAfter` (o bloco em que o foco emenda: estudo/pausa que começa quando este acaba), `formatCountdown`
- `src/infrastructure/deviceSlot.ts` (um valor JSON por uid em `localStorage`, com Map em memória sem browser — a fábrica dos dois abaixo), `infrastructure/hardcoreSession.ts` (a sessão hardcore — é deste dispositivo, não do doc), `infrastructure/pauseSession.ts` (a pausa em andamento, idem: o timer não sobrevive a reload, a pausa sobrevive), `infrastructure/unloadGuard.ts` (o `beforeunload` "certeza que quer sair?" enquanto um estudo hardcore roda; o texto é o do navegador, nenhum aceita mensagem própria) e `infrastructure/extensionBridge.ts` (a ponte com a extensão: `publishBlocking` dispara `study-pets:blocking` com o estado em JSON (payload `v: 2`), `onExtensionQuery` escuta `study-pets:blocking?`, `onBlockingAck` escuta `study-pets:blocking-ack`, e `extensionDetected`/`extensionVersion` leem o atributo que o content script põe no `<html>`)
- `src/infrastructure/avatar/sprite.ts` (o personagem pintado no canvas → `data:` URI, memoizado por aparência; sem DOM devolve vazio), `infrastructure/audio/sounds.ts` (Web Audio, porte fiel, falha em silêncio), `infrastructure/notifications/notifications.ts` (Web Notifications, guardadas), `infrastructure/visibility.ts` (`onVisible`: a página voltou a ficar visível — `visibilitychange` + `pageshow`), `infrastructure/wakeLock.ts` (Screen Wake Lock enquanto o foco está aberto; sem a API, no-op) `infrastructure/download.ts` (Blob + `<a download>`) e `infrastructure/fileText.ts` (ler como texto o arquivo que o usuário escolheu, com `FileReader` de reserva)
- `.env.test` — liga o modo memória pro Vitest; os testes de aplicação que passam pela infra nunca tocam o Firebase
- `src/shared/strings.ts` — textos da UI React. Tudo que migrar pro React escreve texto aqui, não inline
- `src/shared/toast.ts` — o toast (DOM direto, sem React; vira no-op fora do browser). `max-width: calc(100vw - 32px)` e quebra de linha: com `nowrap` e sem limite, "Plano reajustado: -1 estudo · termina às 18:00 · último estudo com 18 min" media 473px numa janela de 393px e sangrava pelas duas beiradas — sumia justamente o começo e o fim das mensagens que explicam o que mudou no plano
- `src/shared/touchScroll.ts` — o que os dois arrastos do plano precisam do navegador: `lockTouchScroll` (segurar o scroll com um `touchmove` não passivo) e `edgeScrollStep` (rolar sozinho perto da borda da tela). Era privado do `useGroupSelection`
- `src/shared/useDismiss.ts` — "clicar fora fecha, Esc fecha", o que todo popover ancorado precisa. Era privado do menu do avatar; o sininho pediu o mesmo
- `src/shared/useWide.ts` — `useWide()`: a janela tem 1100px ou mais (`matchMedia`). É a mesma largura da media query do CSS; os dois andam juntos. O que só existe em tela grande (a coluna da esquerda `.plan-col`, o toggle Dia · Semana e a Semana, o cartão "Agora · Iniciar" e a barra do topo com a engrenagem e o menu do avatar) nem é montado abaixo disso
- `src/styles/app.css` e `login.css` — o CSS que estava no `index.html`, sem mudança. **Tokenizado em 2026-09-07**: nenhuma cor fixa sobrou — tudo é custom property no `:root` do topo (`--bg`/`--bg2`/`--bg3`, `--text`/`--muted`, `--accent` e as variantes `--accent-wash`/`--accent-glow`, `--on-accent` = tinta em cima do accent, `--s0..5`/`--gc0..5`/`--heat0..4`, `--font-ui`/`--font-mono`/`--font-title`…), com o valor escuro de sempre. Cor nova em CSS = token novo no `:root`, nunca hex solto
- `src/styles/themes/` — o tema **Café de casa** (`cafe.css`, fiel ao pacote: creme, papel, sálvia, terracota, Lora), o padrão desde 2026-09-07. Os outros quatro protótipos (noturno, papel, sálvia, misto) foram descartados pelo Tomi e apagados — ficam no histórico do git. O arquivo só vale sob `:root[data-theme="<slug>"]`; o escuro original é a ausência do atributo. Regra: tema muda cor, tipografia, raio, borda e sombra — **nunca geometria** (grid, `--s`, zoom, posições). `src/shared/theme.ts` (`applyTheme`/`readTheme`/`initTheme`; `localStorage` `sp-theme`, `?tema=<slug>` na URL vence e fica gravado; acerta `color-scheme` e a `<meta theme-color>`) e `features/settings/ThemePicker.tsx` (Configurações → Geral → "Aparência"). Tema é preferência **do dispositivo**, não vai pro Firestore. A origem é o pacote `study-pets-cafe-de-casa/` na raiz (do Tomi, fora do git). O escuro é a ausência do atributo
- `src/infrastructure/` — o mundo externo, atrás de interfaces (`ports.ts`: `AuthPort`, `UserRepository`). `AuthUser.provider` (`'google' | 'password'`, opcional — ausente = trate como `'google'`) diz como a conta entrou; usado só pra decidir a reautenticação ao apagar conta
  - `firebase/` — `config.ts` (config pública, sobrescrevível por `VITE_FIREBASE_*`), `auth.ts` (Google e e-mail/senha: `signUpWithEmail` já dispara `sendEmailVerification`, sem bloquear nada; reautenticação ao apagar conta é por popup do Google ou por `reauthenticateWithCredential` com a senha, conforme `provider`), `userRepository.ts` (`users/{uid}`: `initializeFirestore` com cache persistente, `setDoc` **sem merge**, `onSnapshot` atrás de `subscribe`)
  - `memory/` — as mesmas portas sem rede: usuário fixo já logado, documento no `sessionStorage` da aba (sobrevive a reload/HMR, some ao fechar a aba; aba nova = conta nova; `subscribe` é no-op). `auth.ts` também simula um registro de contas por e-mail/senha (só em memória — não sobrevive a reload, como o próprio estado de login/logout hoje); uid deriva do e-mail (`email:<endereço>`), então contas diferentes viram documentos diferentes. É o **modo teste**
  - `index.ts` — escolhe qual usar por `VITE_PERSISTENCE` (`memory` → teste; qualquer outra coisa → Firebase)
- `src/domain/persistence.ts` — `hydrateUserDoc` (documento cru → estado; **é a função explícita de migração**, tolera todo formato antigo), `serializeState` (estado → documento, com `schemaVersion`) e `readDocMeta` (o carimbo de sync). Hoje `schemaVersion` é 4: a lista de sites saiu de `config.hardcore` e virou `config.siteBlock` (v0–v3 migram na leitura, herdando modo/lista/ligado); v3 = o almoço saiu da config e virou uma série de evento (v0–v2 migram na leitura: `config.lunch`/`lunchDur`/`hasLunch` → série diária `ser_almoco` sem âncora — pra cobrir os dias passados e manter os checks batendo —, cada `lunchOverrides[dia]` → exceção na série + avulso naquele dia; `hasLunch` ausente contava como almoçar às 13:00 por 60 min); v2 = pets como instâncias; v0/v1 (pets por espécie, `skills.owl`) migram na leitura; `windowOverrides` e `meta` ausentes têm default
- `.env.example` — variáveis suportadas (todas opcionais). `.env.teste` liga o modo memória pro `npm run dev:teste`
- `src/domain/` — regras puras em TypeScript, sem DOM/Firebase/estado global:
  - `types.ts` — tipos do domínio (`StudyBlock`, `UserConfig`, `RecurringEventSeries`, `CheckRecord`, ...)
  - `avatar.ts` — o personagem como dados: a arte em grid de letras (24×42, 4 frames), o catálogo de tons de pele, cores e penteados, a paleta e o contorno automático. Puro — ver "O personagem"
  - `time.ts` — `dk`, `timeToMins`, `minsToTime`, `mondayOf` (tudo em horário local) e **`blockMins`** (a duração que vale de um bloco: fim − início − pausado; a única conta de duração do app — um teste varre `src/` atrás de `endTime − time` solto)
  - `weeks.ts` — as semanas que a UI mostra: `buildWeeks` (a partir de hoje, do período configurado e dos dias com dados — sem data hardcoded), `weekDays`, `dateForWeekDay`, `findWeek` e os tipos `Week`/`WeekDay`. Quem chama é `application/plan.ts` (`rebuildWeeks`)
  - `config.ts` — `DEFAULT_CFG` e `migrateConfig`
  - `planner.ts` — `generateBlocks(cfg, events, pauses)` (a memoização fica em `application/plan.ts`; as pausas registradas esticam o bloco que as contém e empurram o resto do dia) e `calcActualEnd`
  - `events.ts` — `expandEventsForDate`, com semanal/quinzenal/mensal e exceções
  - `progression.ts` — XP, moedas, `LEVELS` (do usuário), o catálogo `SKILLS` (com o `tier` de cada uma, `SKILL_TIERS` e a constante `SKILL_DAY_SHARE` que iguala a força das skills — ver "Sistema de skills") e `skillEligible`/`skillBonus`/`bonusForCheck`
  - `checks.ts` — quem pode ser marcado (`canCheckBlock`: dia fechado é read-only, dia futuro não
    chegou), `computePendingPetXP` (o XP dos pets, idempotente) e a caminhada pra trás nos dias
    (`previousDayKey`, `previousCountingDay` — folga não conta, igual em `allDays()`; é o que as
    skills Recomeço e Descansado usam pra distinguir sumiço de descanso)
  - `stats.ts` — `computeStats` (uma passada só) e `calcStreaks`. `dayXP`/`dayCoins` são o recorte por dia do que entrou nos totais — é deles que o sininho tira "ontem entrou na conta: +310 XP"
  - `groups.ts` — grupos de estudo: pertencimento por horário (`blockInGroup`), `groupProgress`, `validateGroupRange`, `groupHeaderPositions`
  - `auth.ts` — `isValidEmail`/`isValidPassword` (mínimo 8 caracteres — o Firebase aceita 6, a gente pede mais), `AuthError` (motivo tipado: `email-in-use`, `invalid-credential`, `weak-password`, `invalid-email`, `too-many-requests`, `network`, `unknown` — nunca o código cru do Firebase vaza pra UI) e `authErrorReasonFromCode`, que mapeia os códigos do Firebase Auth pro motivo (junta de propósito `auth/wrong-password`/`auth/user-not-found` (SDKs antigos) e `auth/invalid-credential` (SDKs novos) no mesmo motivo — nunca revela se foi o e-mail ou a senha que errou)
- `tests/` — Vitest sobre o domínio e a infra em memória
- `e2e/` + `playwright.config.ts` — smoke test de ponta a ponta no modo teste
- `public/idle/` — sprites (era `idle/` na raiz). O Vite copia `public/` pro `dist` preservando os caminhos, então o código continua pedindo `idle/user/0.png`
- `public/idle/pets/{form}/` — convenção pra sprites de pets (uma pasta por **forma**, 25 hoje: as 5 espécies `dog`, `cat`, `snake`, `cow`, `dove` e as 4 formas de evolução de cada — ver "Evolução" em "Sistema de pets")
- Sprites são frames sequenciais nomeados `0.png`, `1.png`, ...
- `firestore.rules` — regras de acesso (só o dono lê/escreve `users/{uid}`)
- `scripts/backup-firestore-console.js` — snippet pra baixar seu doc do Firestore pelo DevTools (pra quem não é o autor, o botão "Baixar meus dados" em Configurações → Geral faz o mesmo)
- `scripts/app-icon.mjs` — gera os ícones do PWA em `public/icons/` **e o ícone da extensão** (`extension/icon-192.png`) a partir do personagem (`node scripts/app-icon.mjs`, Chromium headless do Playwright). O da extensão sai daqui porque era uma cópia manual que envelhecia calada quando o personagem mudava; tamanho novo no manifest dela = alvo novo em `targets`. Arte autoral; um ícone à mão substitui os PNGs
- `public/manifest.webmanifest` e `public/icons/` — o manifest do PWA (estático) e os ícones 192/512/512-maskable/apple-touch
- `scripts/character-sprites.mjs` — os PNGs do personagem padrão em `public/idle/user/` (o app desenha o dele na hora; estes servem ao `app-icon.mjs`). `--ascii` mostra os penteados no terminal
- `scripts/png.mjs` — o encoder de PNG RGBA sem dependência (zlib do Node + CRC32), compartilhado por `pixel-sprites.mjs` e `character-sprites.mjs`
- `scripts/bestiary.mjs` + `docs/bestiario.html` — o **bestiário**: gera a página do elenco inteiro a partir do catálogo (ver "Bestiário")
- `docs/registro-tratamento.md` e `docs/plano-de-incidente.md` — os dois documentos **internos** de proteção de dados (não vão pro ar): o registro do art. 30 do GDPR e o procedimento das 72 horas. Ver "Os documentos legais"
- `public/legal/` — os **três documentos** (`privacidade.html`, `termos.html`, `impressum.html`) e o `legal.css` deles. HTML estático, fora do app — ver "Os documentos legais"
- `docs/juridico.html` — a **revisão jurídica e o plano de abertura**: os 91 achados separados em jurídico / contratual / produto, e as quatro fases pra abrir o app pro público. Página autocontida (abre offline, sem build), escrita à mão — não há script que a gere. Vale enquanto o app for gratuito e sem anúncio; **entrar dinheiro muda a Fase 3 inteira** e pede advogado de verdade
- `scripts/ts-loader.mjs` — o hook que deixa um script do Node importar o domínio direto (`./time` → `./time.ts`); o Node 24 já tira os tipos sozinho, só a resolução faltava. Usar com `node --import ./scripts/ts-loader.mjs <script>`
- `scripts/pixel-sprites.mjs` — desenha em código os sprites placeholder das 25 formas (`node scripts/pixel-sprites.mjs` → `public/idle/pets/{form}/`). Cada forma é uma função `(frame) → grid`; as evoluções reaproveitam o corpo da família (`dogBody`, `catBody`, `snakeCoil`+`snakeHead`, `cowBody`, `doveBody`+`doveHead`) e mudam paleta + detalhes, e a forma-base de cada família sai pixel a pixel igual à anterior. Arte autoral, 32×32 com transparência, no padrão do personagem; trocar por arte à mão quando houver. Teste garante que toda forma do catálogo tem os 4 frames no disco
- `extension/` — a extensão do navegador do **bloqueio de sites** (Chromium, Manifest V3, sem build; carregar sem compactação — o README de lá é escrito pro usuário): `content.js` (se anuncia no `<html>` com a versão, repassa o estado que o app publica, devolve o ack, pergunta ao carregar), `background.js` (guarda em `chrome.storage.local`, aplica as regras do `declarativeNetRequest`, responde com o ack, acende a badge "ON", alarme pro fim do bloco, redireciona abas já abertas e vigia `tabs.onUpdated`), `rules.js` (puro: `buildRules`/`isBlocked`/`ackFor`/`isLive`/`isSupported` — testado em `tests/extension-rules.test.ts` e `extension-background.test.ts`, tipos em `rules.d.ts`), `blocked.html/css/js` (a tela do pet no lugar do site: sprite animado com os frames servidos pelo próprio app, contagem, "Voltar pro Study Pets"; a frase de saída muda entre hardcore / estudo normal / teste; sem botão de desbloquear) e `popup.html/css/js` (o que o ícone abre)
- `e2e/extension.spec.ts` + `playwright.extension.config.ts` + `scripts/chromium-exe.mjs` — a extensão num navegador de verdade (`npm run test:ext`)
- `dist/` — saída do build, não versionada

## Workflow

1. `npm run dev:teste` sobe o app em `http://localhost:5174` em **modo teste**: sem Firebase, já logado num usuário fixo, cada aba nova começa como conta nova (onboarding aparece); os dados vivem na aba (sobrevivem a reload, somem ao fechar). É o jeito rápido de testar qualquer fluxo sem tocar em dados reais
2. `npm run dev` sobe em `http://localhost:5173` com Firebase real (`localhost` já é domínio autorizado no Firebase Auth) — pra validar login e persistência de verdade
3. Não existe mais cópia pra replicar: uma base só, dois modos por ambiente
4. `npm test` (Vitest) e `npm run typecheck` antes de commitar
5. `npm run test:e2e` roda o smoke test (Playwright, `e2e/smoke.spec.ts`) contra o modo teste — os fluxos principais, cada um numa aba nova com o relógio fixo. Sobe o servidor sozinho. **Rodar antes de mexer em UI.** O viewport padrão é 480×900 (o app é mobile-first), e só alguns `describe` sobem pra tela grande (`na Semana`, `no laptop`) — **mexeu no layout B7, escreva o teste lá**: foi por esse buraco que os dois bugs de posição do tour entraram. Usa o Chromium do próprio Playwright em headless (`npx playwright install chromium` uma vez). **Nunca o Chrome/Edge do sistema nesta máquina**: o Cold Turkey Blocker fecha qualquer `chrome.exe`/`msedge.exe` sem a extensão dele — levando as abas reais junto. `npm run test:e2e:ui` e `--headed` usam o Chromium completo, que ele pode pegar pelo nome; avisar antes
6. `npm run test:ext` roda a extensão do navegador num Chromium de VERDADE (`e2e/extension.spec.ts`, `playwright.extension.config.ts`): extensão carregada, site de mentira em `.test` resolvido pro servidor local, regra do declarativeNetRequest aplicada. Precisa do Chromium **completo** (o headless shell do smoke não carrega extensão) — `scripts/chromium-exe.mjs` resolve o binário, e **dentro do app desktop do Claude o caminho real do MSIX é obrigatório** (`AppData\Local\Packages\Claude_…\LocalCache\Local\ms-playwright`; pelo caminho virtual o Windows não acha o manifesto SideBySide e sai `spawn UNKNOWN`). `SP_CHROMIUM_EXE` sobrescreve. Continua valendo: **nunca o Chrome/Edge do sistema**
7. `npm run build` gera o `dist/`
8. Commit + push → Vercel builda e publica sozinho

## Princípios arquiteturais

Mais importantes que valores concretos. Estes você protege ao mexer no código:

- **Regra pura mora em `src/domain/`**: se uma função só transforma dados em dados, ela vai pro domínio, é tipada e ganha teste. O domínio **não pode** tocar DOM, React, Firebase, `state`, áudio, notificação ou `new Date()` implícito — quando precisa do "agora", recebe como parâmetro (ver `SkillContext` em `progression.ts`). Efeito (estado, persistência, timers, toast) fica em `src/application/`; React só formata e chama casos de uso.
- **Camadas em uma direção**: `features` → `application` → `domain`/`infrastructure`. Componente não importa `infrastructure` nem muta `state` direto; caso de uso não importa React nem toca o DOM (exceção pequena e nomeada: `shared/toast`).
- **Estado centralizado**: tudo em um único objeto `state`, que vive em `src/store/store.ts`. Não criar globais soltas. Modal aberto por clique local é `useState` do componente; só o que um caso de uso abre (foco, onboarding, fim do dia) vai pro `derived`.
- **Cor é token**: `app.css` e `login.css` não têm hex/rgba solto — toda cor é uma custom property do `:root`, e um tema é só `:root[data-theme=…]` redefinindo tokens (mais uns poucos ajustes de forma). Quem precisa de uma cor nova cria o token com o valor escuro e o redefine nos temas.
- **Ids e classes são contrato**: o CSS em `src/styles/app.css` e o smoke test em `e2e/` dependem dos ids/classes que os componentes rendem. Mudar um nome = mudar nos três lugares.
- **`div` que responde a clique responde a teclado**: `role`, `tabIndex={0}`, `aria-label` e Enter/Espaço. Vale pra `.block-row` (`role=button`) e pro `.check` (`role=checkbox` + `aria-checked`) — as duas ações mais usadas do app eram só de mouse e dedo até 2026-09-10 —, e pro `#active-pet-card`, que já fazia certo. O foco aparece com `:focus-visible`.
- **Sem `notify()` por segundo**: o timer e o modo foco derivam o restante do relógio dentro do componente (`useSecondTick`). Um `notify()` global a cada segundo faria o app inteiro re-renderizar e o `computeStatsNow` recalcular.
- **Checks por horário, não por índice**: a chave dos checks é o `time` do bloco (`"09:00"`). Isso evita corrupção quando a config muda. Valor é `{ pet: instanceId | null, bonus: number }` — `pet` = **id da instância** equipada no momento do check (o pet adotado, não a espécie; pets migrados do formato antigo têm id igual ao da espécie, então checks antigos continuam batendo); `bonus` = multiplicador aditivo de XP (0, ou 0.05–0.15 conforme o nível do pet) decidido na hora pelas skills ativas. Retrocompat: `true` antigo é tratado como `{ pet: null, bonus: 0 }` por `checkPet()` / `xpFromCheck()`.
- **Memoization em `generateBlocks`**: a função tem cache (chave = config do dia + eventos + pausas). Sempre que alterar config, eventos ou as pausas de um dia, chamar `clearBlockCache()`.
- **Mexer no gerador reescreve o passado**: nada de plano é salvo. `computeStats` regenera cada dia a partir da config, dos eventos e das pausas, toda vez — então mudar `planner.ts` muda o histórico de dias já encerrados, retroativamente. As **chaves de check sobrevivem** (são por horário, e nenhuma mudança até hoje moveu o início de um bloco), mas um bloco que se **divide** faz um dia fechado perder minutos de "feito": o bloco novo nasce sem check. Um dia que batia a meta por pouco pode cair fora dela, e a sequência quebra sozinha — exatamente o que o app promete não fazer. Por isso, antes de mexer no gerador: gere o plano com a versão anterior (`git show HEAD:src/domain/planner.ts`) e com a nova, e **compare** — minutos de estudo por dia, chaves órfãs, blocos que encolhem — varrendo janelas × ritmos × eventos × pausas, não só rodando a suíte. Na mudança de 2026-09-10 (a sobra do fim da janela) isso mostrou que o dia real de uma conta com a refeição das 13h sai byte a byte igual, e que o estrago fica restrito a configs que ninguém usa.
- **Duração é `blockMins`, sempre**: um bloco esticado por uma pausa do timer (09:00–09:32 com `paused: 7`) vale 25 min. XP, moedas, meta, aderência, grupos e o anel do foco passam por `blockMins` (`domain/time.ts`); `endTime − time` solto infla tudo em exatamente os minutos pausados, e um teste em `tests/pauses.test.ts` reprova qualquer arquivo de `src/` que faça a conta por conta própria.
- **Stats em uma passada**: `computeStats()` calcula tudo de uma vez iterando os dias uma única vez. Não criar funções separadas que reiteram.
- **Datas dinâmicas**: sem dates hardcoded. As semanas são construídas a partir da semana atual.
- **Último bloco do dia é sempre estudo** (nunca termina em pausa).
- **Tratamento de erro do Firebase**: load/save dentro de try/catch.
- **Nada é salvo antes de o documento ser lido**: `users.save` substitui o documento inteiro, então o boot é crítico. `loadUserData` devolve `'new' | 'loaded' | 'failed' | 'stale'` e só chama `blockSaves(false)` no caminho feliz; na falha trava o save, zera o estado e liga `derived.loadFailed`, e o `App` troca a árvore pela tela `#load-failed-screen` (com "Tentar de novo" e "Sair da conta" — sem a segunda, uma falha permanente prende o usuário, porque o botão Sair mora no cabeçalho que a tela substituiu). Enquanto o documento não chega, `derived.booting` mostra `#boot-screen` em vez do `#app`: antes o app aparecia clicável e gerado do `DEFAULT_CFG`, e um check marcado nessa janela era jogado fora sem uma palavra.
- **O load que volta atrasado não manda em nada**: `loadUserData` marca uma geração (`++geracao`) e, depois do `await`, confere que ela ainda é a atual **e** que `state.user.uid` ainda é o mesmo — senão devolve `'stale'` e sai sem tocar em estado, trava ou inscrição. Sem isso, sair no meio de um load lento e entrar com outra conta fazia a resposta da primeira cair por cima da sessão da segunda, e o primeiro check gravava o histórico de uma no documento da outra. `resetToLoggedOut` também usa `Object.assign(state, emptyPersistedState())` em vez de uma lista de campos: a lista esquecia `eventSeries`, `groups`, `windowOverrides`, `avatar` e `tutorialSeen`.
- **Firebase só atrás de porta**: nada fora de `src/infrastructure/firebase/` importa `firebase/*`. O app fala com `auth` e `users` (ver `ports.ts`); é isso que permite o modo teste em memória e, no futuro, emulador ou outro backend sem tocar na UI.
- **Todo formato antigo do Firestore passa por `hydrateUserDoc`**: campo novo no doc = default ali + teste em `tests/persistence.test.ts` com o doc sem o campo. `serializeState` é o único lugar que monta o documento salvo.
- **Save substitui o doc inteiro; sync por snapshot com carimbo de escrita**: `users.save` é `setDoc` **sem merge** (o merge do Firestore é profundo — chave que sumiu de `checks[dia]` ficava no servidor e voltava no reload). Cada save leva `meta: { writer: CLIENT_ID, writtenAt }`; o snapshot que volta com o nosso `writer` é eco e é ignorado (`application/sync.ts`). O doc remoto só entra se não há save local pendente (v1: o local vence) e o onboarding não está aberto; nunca mexe em `derived` (timer, foco).
- **Overrides por dia são a config daquele dia, nunca regra no gerador**: `windowOverrides` entra em `blocksForDay` via `configForDay`; a refeição e qualquer outro bloqueio são **eventos** (avulsos ou séries), nunca campo de config. Quem precisa dos blocos de um dia como eles eram (estatísticas, XP do pet, prompt de fim de dia) chama `blocksForDay` — nunca `generateBlocks(state.config, …)` direto.

## Conceitos do app

> As seções abaixo descrevem as **regras** do produto e ainda citam nomes do app vanilla (`renderBlocks`, `toggleCheck`, `renderXP`, `applyPendingPetXP`…). Tudo migrou: a regra continua valendo, e o código equivalente está em `src/domain` (o cálculo), `src/application` (o caso de uso) e `src/features/<feature>` (a tela). Na dúvida, o código é a fonte da verdade.

- **Ciclos**: pomodoros separados por pausa longa ou evento (refeição inclusive) viram "ciclos" coloridos (classes `.s0` a `.s5`; no código, `block.cycle`). **Chamavam-se "sessões" até 2026-09-11** — o app usava a mesma palavra pra duas coisas incompatíveis: a leva de quatro pomodoros ("Sessão 2" na lista) e a conta inteira ("Cancelar sessão", que apaga tudo e volta pro onboarding). "Ciclo" é o nome clássico do Pomodoro (4 pomos + pausa longa) e libera "sessão" pro segundo sentido, que ficou. O rename foi de vocabulário, não de comportamento: textos, campo do bloco, `cycleStats`, classes (`.cycle-block`, `.cycle-divider`, `.cd-line`/`.cd-label`, `.cycle-xp`, `.now-cycle`) e tokens (`--on-cycle`, `--fade-done-cycle`, `--fade-event-cycle`). Duas fronteiras traduzem em vez de renomear: `tests/_legacy-stats.mjs` (a cópia literal do código pré-migração, que continua lendo `b.session` — quem traduz é o wrapper do teste de equivalência) e `HardcoreSession.cycle`, que aceita o `session` antigo na leitura porque a sessão do hardcore vive no `localStorage` do dispositivo.
- **O ciclo fecha** (2026-09-11, `domain/cycles.ts`): marcar o último estudo de uma leva reconhece a leva inteira — o divisor vira "CICLO 1 ✓ · 1H40" em verde (`.cycle-divider.done`) e uma faixa comemora (`#cycle-cheer` na lista, `.focus-done.cycle` com o sprite do pet no modo foco, onde o fim do bloco já era um momento). É o marco que faltava entre o check de um bloco e o fim do dia: quem estuda das 9 às 18 colhia feedback de bloco a bloco e só via o agregado — XP creditado, pet subindo — nove horas depois.
  - **Não é um botão de encerrar**, de propósito: um botão sempre presente ou pede cinco decisões por dia ou deixa a leva pendurada aberta — dívida, o oposto do que a feature quer —, e teria de escolher entre travar os blocos que faltam (rigidez) ou não significar nada. O momento já existe no dia da pessoa (marcar o último pomo antes da pausa longa, quando ela levanta da cadeira); o app só não o reconhecia.
  - **Não credita nada e não trava nada**: o XP continua entrando só quando o dia fecha (é o que impede marcar e desmarcar pra repagar), então a faixa diz "+200 XP **no fim do dia**". Desmarcar continua valendo, e a leva só volta a fechar quando estiver completa de novo.
  - **A pergunta é sobre a leva, não sobre a linha**: quem marca fora de ordem fecha no penúltimo horário, e o app reconhece igual — diferente do `completesGroup` das skills, que é posicional de propósito porque ali há XP em jogo. O preço é ver a animação de novo ao desmarcar e remarcar; sem XP envolvido, é inofensivo.
  - **Leva de um bloco só não ganha faixa** (`MIN_BLOCKS_TO_CHEER`): um evento entre duas janelas nasce sozinho num ciclo dele, e o check já tem o próprio feedback. O divisor verde, esse vale sempre — é estado, não evento.
  - **No foco a faixa é a única com o pet** e fica mais tempo (6,5 s contra 4 s), no lugar de "✓ Estudo 4 concluído"; a emenda na pausa longa segue como sempre — pedir "começar a pausa ou sair" seria fricção no exato momento da comemoração. Se o bloco já estava marcado à mão, a lista já comemorou e o foco não repete. Ids/classes: `#cycle-cheer` (`.sc-title`/`.sc-sub`), `.cycle-divider.done`, `.focus-done.cycle` (`.fd-pet`/`.fd-title`/`.fd-sub`). e2e **48** (lista) e **49** (foco).
- **Janelas de estudo** (`config.studyWindows`): lista de intervalos `[{start, end}]` durante o dia em que o usuário estuda. UI na aba Rotina = uma linha por janela dentro de um card (`.sw-row`: início → fim, duração alinhada à direita, ✕ pra remover), botão "+ Adicionar" no cabeçalho da seção. Permite N janelas (caso de uso clássico: estuda 9-12 e 15-20, com gap de 3h no meio onde o app não gera pomos). `generateBlocks` itera as janelas em ordem; entre janelas, bloqueios (almoço/eventos) aparecem visíveis no plano. `cfg.start` / `cfg.end` viraram **campos derivados** (primeira janela / última janela) — mantidos no schema só pra retrocompat. Migração: `migrateConfig(cfg)` cria `studyWindows: [{start, end}]` a partir do antigo `start`/`end` se ausente. Pra estender o dia via "Prolongar estudos" do end-of-day prompt: ajusta `end` da última janela.
- **Eventos**: blocos representam compromissos (aulas, consultas, treino). Schema `{name, start, end, countsAsStudy}` (default `true` pra retrocompat). Quando `countsAsStudy=true`: tipo `'event'` no resultado de `generateBlocks` — herda cor do ciclo (borda dashed), tem check, dá XP/moedas pela duração real, alimenta streak + pet XP. Quando `countsAsStudy=false`: tipo `'intervalo'` — bloqueia o tempo no plano (cinza, dashed, sem check, sem XP). Ambos clicáveis pra apagar. Painel `#event-panel` tem checkbox "Conta como estudo (dá XP e moedas)" default marcado. **Editar**: tocar no evento abre o modal `#event-delete-confirm`, que tem "✏️ Editar" (`#event-edit-btn`) → o mesmo `#event-panel` em modo edição, preenchido (`updateEvent` substitui o avulso pelo início antigo; `updateSeries` troca nome/horário/dias/frequência/até/conta-como-estudo da série inteira, mantendo `id`, `anchor` e `exceptions`). Avulso não vira série ao editar; editar uma ocorrência só é o chip "Só este dia" (exceção + avulso, automático). **`'intervalo'` agora vem APENAS de evento sem countsAsStudy** — `extraBreaks` na config foi removido. **Atalhos** (2026-09-06): chips no topo do "Novo evento" — 🍽️ Refeição (13:00–14:00, repete todos os dias), 👥 Reunião, ☕ Café, 🚌 Deslocamento, ✏️ Outro (`#ev-presets`, `data-preset`) — só preenchem o formulário (`domain/eventPresets.ts`); nome que já começa com emoji não ganha o 📅 do plano. **A refeição é um evento**: o almoço saiu da config (série diária `ser_almoco`, sem XP; conta nova nasce com ela às 13:00 no `finishOnboarding`; "almocei mais cedo hoje" = tocar na refeição → Editar → "Só este dia"). **Editar só um dia da série** (`updateSeriesOccurrence`): o dia vira exceção e ganha um avulso; o chip "Aplicar a" (`#ev-scope`, `data-scope`) no painel de edição escolhe entre isso e a série inteira — padrão "só este dia", e nesse modo a recorrência fica escondida. e2e **31**.
- **Arrastar eventos**: evento e refeição se movem com a mão — o resto do plano é gerado, então "arrastar um estudo" não teria significado. **No Dia** (qualquer largura): a linha ganha uma alça `⠿` (`.ev-grip`) na ponta direita, que pega com o dedo ou com o mouse; no mouse dá pra pegar a linha inteira, e o arrasto só começa depois de 6px, então o clique continua abrindo `#event-delete-confirm`. A linha vira fantasma (`.block-row.dragging`) e uma caixa tracejada (`#drag-ghost`) mostra onde ela cai, com o horário escrito. **Na Semana** (≥1100px): o bloco se arrasta pela grade, pra outro horário **e pra outro dia** (`#wv-ghost`; as colunas carregam `data-day-key`/`data-from`/`data-to`, e dia de descanso fica de fora — soltar lá esconderia o evento). Passo de **5 min**, duração preservada, ponto de pega respeitado (pegou no meio, continua no meio). **A geometria é medida uma vez, no começo do gesto** (`domain/eventDrag.ts`): mover o evento muda o plano em volta, e re-medir a cada quadro faria o alvo fugir do dedo. Numa série, soltar abre `#event-move-scope` — "Só este dia" (exceção + avulso) ou "Toda a série" —, a mesma pergunta do chip "Aplicar a"; **trocar o dia da semana da série inteira é recusado** (isso é edição, não arrasto), então arrastar uma ocorrência pra outro dia vale só pra ela. Dia encerrado não move; **dia futuro move** (planejar o amanhã é o ponto) — nota: tocar num evento de dia futuro ainda cai na recusa "Ainda não chegou 🔮", inconsistência conhecida (ver o plan file). Soltar um evento em cima de outro esconde o de baixo do plano daquele dia — comportamento antigo do `generateBlocks`, não do arrasto. Convive com a seleção de trecho sem disputa (botão direito e toque longo continuam sendo grupo; durante uma seleção o arrasto fica desligado), e o clique que fecha um arrasto é engolido. e2e **41/42/43/44**.
- **Eventos recorrentes**: ao criar evento, o checkbox "Repetir este evento" expande seção com chips de dia da semana (multi-select, pré-seleciona o dia atual), radio de frequência (toda semana / a cada 2 semanas / mensalmente) e input "Até" opcional. Série fica em `state.eventSeries` (array paralelo a `state.events`); avulsos continuam em `state.events`. Helper `getEventsForDate(dateKey)` une as duas fontes — ocorrências de série ganham `_seriesId` no objeto. **`blocksForDay` e `applyPendingPetXP` chamam `getEventsForDate`**, então stats/check/XP-do-pet funcionam transparentemente. Biweekly conta paridade pela semana da `anchor` (`mondayOf(anchor)` vs `mondayOf(date)` em weeks); monthly bate `date.getDate() === anchor.getDate()` (dias 29-31 podem pular meses curtos — by design). Apagar evento de série abre modal com 2 opções: "Só este dia" (empurra `dateKey` em `series.exceptions`) ou "Apagar a série" (remove a entrada de `state.eventSeries`). Avulsos mantêm botão único "Apagar".
- **Importar calendário (.ics)** (2026-09-10): a grade do semestre não precisa ser digitada evento por evento. Configurações → Geral → "Importar calendário" (`CalendarImportSection`) aceita o arquivo que o Google Agenda, o Outlook, o Apple Calendar e os sistemas de faculdade exportam. É **upload**, não assinatura: sem backend, sem OAuth, sem CORS — o parser é puro (`domain/ics.ts`) e roda no navegador. Sync ao vivo ficou de fora conscientemente (a URL secreta do Google é bloqueada por CORS e pediria uma função serverless na Vercel — o primeiro backend próprio do app; a Calendar API pediria verificação do Google).
  - **Tudo vira evento**, que é o modelo que já existe: uma repetição que cabe na série do app (`weekdays` + `weekly`/`biweekly`/`monthly`) vira **série**; o que não cabe (a cada 3 semanas, "terceira quinta do mês", anual) vira **lista de datas**, expandida na leitura — não é perdido nem torto. `EXDATE` vira `exceptions`; `RECURRENCE-ID` (a aula que mudou de sala num dia só) cai exatamente no que o app já faz pra "editar só este dia": exceção na série mãe mais um avulso naquele dia.
  - **O horizonte começa hoje.** Um .ics de semestre carrega agosto junto, e mexer em dia já fechado não é importar, é reescrever histórico. Vai até o `periodEnd`, ou um ano. A âncora da série é a **primeira ocorrência que entra**, o que preserva a paridade da quinzenal sem trazer o passado.
  - **Fuso é resolvido na leitura**: `DTSTART` em UTC ou com `TZID` vira horário local (`Intl.DateTimeFormat` acha o deslocamento, duas passadas por causa do horário de verão) — senão a aula das 10h em Munique chegaria às 8h. O app é local em todo lugar. O `until` da série sai da **última ocorrência de verdade**, não do `UNTIL` cru, que é um instante e escorregaria um dia ao virar data local.
  - **Nada entra sem revisão** (`#ics-review-panel`): uma linha por compromisso com o que o app entendeu da repetição, o que ficou de fora agrupado por motivo (dia inteiro, cancelado, sem duração, já passou), e o botão diz quantos compromissos vão entrar. Calendário pessoal é cheio de aniversário e feriado; importação silenciosa encheria o plano de ruído.
  - **XP é escolha, não suposição**: o .ics não distingue aula de consulta médica, e XP inflado corrompe stats e moedas pra sempre — então o padrão é `countsAsStudy: false` (só reserva o tempo), com o botão `#ics-all-xp` ("✦ XP") ligando a grade inteira num clique.
  - **Reimportar substitui, não duplica**: cada item entra carimbado com `externalId = ics:<UID>`, e aplicar começa apagando o que tem o mesmo carimbo. Evento digitado à mão não tem carimbo, então nenhuma importação encosta nele; e importar um segundo calendário não apaga o primeiro. Campo novo no doc, opcional e sem migração (`events`/`eventSeries` já passam inteiros por `hydrateUserDoc`) — `schemaVersion` continua 4.
  - **Duas portas, porque uma só não foi achada** (2026-09-10, depois de o Tomi não achar o botão): a seção é a **segunda do Geral**, logo abaixo de "Período de uso" — importar é montar o dia, não exportar dado, e no porão junto de "Meus dados" ninguém rolava até lá. E o **"+ Evento" tem a porta** (`#ev-import`, `.ev-import-link`, só no evento novo — editando não há o que importar): fecha o modal e chama `requestSettings('calendar')`. Aí `derived.settingsRequest` carrega `{ focus }` (era um booleano), a `SettingsPage` abre no Geral, rola até a seção e acende `.st-section-lit` por 2,4 s. É onde a cabeça já está em "botar compromisso no plano".
  - **Dia inteiro fica de fora**: sem horário, viraria bloqueio do dia todo. Quem atravessa a meia-noite entra até 23:59, com a nota na revisão. Ids/classes: `#ics-import-btn`, `#ics-file`, `#ics-review-panel`, `#ics-all-xp`, `#ics-import-confirm`, `.ics-item`, `.ics-xp`. e2e **39**.
  - **V1 consciente**: sem assinatura por URL (sync ao vivo), sem "apagar tudo que veio da importação" (some evento por evento, como qualquer outro) e sem escolher quais calendários de um arquivo com vários entram — a revisão já deixa desmarcar linha a linha.
- **Bloco atual**: o que está acontecendo no horário real ganha destaque visual.
- **Sobra menor que um pomo** — antes de um evento (a refeição é um deles) **e no fim da janela** (desde 2026-09-10; até então a sobra no fim da janela era jogada fora: 20 min com pomo de 25 não viravam nada): sobra ≥ metade do pomo vira **mini-estudo** (`mini: true`, XP pela duração real); menor que isso **estica o último estudo** — mas só se ele termina exatamente onde a sobra começa (senão passaria por cima da pausa/evento entre os dois; uma sobra pequena logo depois de um bloqueio fica livre). No fim da janela a conta é feita depois da pausa do pomodoro: cabe a pausa e ainda sobra meio pomo → pausa + mini (09:00–09:50 → 25 + 5 + mini de 20); senão a **pausa não é emitida** e a sobra inteira vira mini ou estica o último estudo (09:00–09:35 → um estudo de 35 min). **Isso inclui o dia que fecha em ciclo** (mudou em 2026-09-10, tarde): o padrão 09:00–18:00 fecha 4 ciclos redondos, a pausa longa das 17:40 preenchia exatamente até as 18:00 e era descartada pelo laço final ("o último bloco é sempre estudo") — levando 20 minutos junto, e fazendo o próprio app avisar da própria config em "Como fica o dia". A condição que a emitia ("encosta exatamente no limite") nasceu pro caso do **evento** e vazava pro fim da janela, onde não há nada em que encostar: `nextStartAfterBreak` cai em `winEnd` quando não há bloqueio. Agora os dois casos são ramos separados. Sem pausa emitida **não há ciclo novo** (o `cycleN++` só corre quando a pausa longa sai de fato), senão o último estudo do dia nascia sozinho num divisor só dele — e isso apareceria na Análise → "Conclusão por ciclo". A conta de "cabe meio pomo depois da pausa?" é feita com `place`, não com `cur + breakDur`, porque uma pausa registrada do timer que caia dentro da pausa do pomodoro também a estica. Medido: em 690 combinações de janela × ritmo, nenhuma deixa tempo morto (antes eram 16 285 minutos no total), e nenhum plano perde minuto de estudo. **O dia real de uma conta com a refeição das 13h sai byte a byte igual ao de antes** — a refeição já quebrava o ciclo —, então o histórico de quem já usa o app não se mexe; a correção aparece no preview sem eventos, nas janelas noturnas e em quem apagou a refeição. Antes de um evento a pausa do pomodoro **nunca** invade o bloqueio: se ela não cabe, some, e o plano vai direto pro bloqueio (inclusive quando o pomo termina exatamente no minuto em que o evento começa) — e dois estudos podem ficar colados pra caber um inteiro antes do evento (decisão de 2026-09-03).
- **Datas**: WEEKS construído dinamicamente. Sem `periodEnd` (modo "sempre"): até 31 de dezembro do ano atual (mínimo de hoje+8 semanas, pra cobrir virada de ano); expande pra frente se houver dados futuros. Com `periodEnd` definido: cobre **exatamente** o intervalo escolhido — limite real, sem expansão pra frente (dados fora do período ficam guardados no Firestore mas não aparecem na UI; reabrir o período os traz de volta). Em ambos os casos, expansão pra trás se houver dados antigos.
- **Login por e-mail/senha**: a tela de login (`#login-screen`) tem um formulário curto acima do botão do Google — e-mail, senha, botão principal que muda de texto conforme o modo (`#login-toggle-mode` troca entre "Entrar"/"Criar conta" e "Já tenho conta"/"Criar conta"), "Esqueci a senha" (`#login-forgot`, usa o e-mail já digitado) e erro inline (`#login-error`, nunca toast) abaixo do formulário. Enter envia (é um `<form>`). Validação de formato/senha mínima (8 caracteres — o Firebase aceita 6) acontece **antes** de chamar a infra, em `application/session.ts` (`signUpWithEmail`/`signInWithEmail`/`resetPassword`, todos devolvendo `{ok:true} | {ok:false, reason}`). Motivo de erro nunca é o código cru do Firebase — vira um dos `AuthErrorReason` de `domain/auth.ts`, com texto em `strings.login.errors`. **E-mail já cadastrado (com Google ou com senha) devolve `email-in-use`** com o texto "Já existe uma conta com este e-mail. Entre com o Google ou use 'Esqueci a senha'." — não tenta vincular os dois provedores (`linkWithCredential`) por decisão consciente (ver plan file). **"Esqueci a senha" sempre mostra a confirmação** (`#login-reset-sent`, "Enviamos um e-mail pra …") pra e-mail com formato válido, mesmo que a conta não exista — não revela quais e-mails têm conta. **Verificação de e-mail é opcional**: `signUpWithEmail` já dispara `sendEmailVerification` sem bloquear nada, mas não há lembrete no perfil pra quem não verificou (fora do escopo — ver plan file).
- **Onboarding**: na primeira vez que um usuário loga (Firestore doc não existe), abre o modal em três passos. (1) **Personagem** (2026-09-10, `#onb-avatar`, "🙋 Quem vai estudar") — os mesmos controles das Configurações (`features/avatar/AvatarControls`, controlado: preview animado, tom de pele, cor de cabelo, penteado e corpo), porque a primeira coisa que a pessoa vê do app deve ser dela. A escolha fica num `useState` e só é aplicada por `finishOnboarding` — `setAvatar` salvaria na hora, e um documento criado antes do onboarding terminar faria um reload pular o pet inicial. Aparece na mesma condição do pet (conta nova ou sessão recomeçada); "← Mudar personagem" (`#onb-avatar-back`) volta do passo do pet. (2) **Pet inicial** — só quando `pets.owned` está vazio (conta nova, ou depois de Cancelar sessão): um card por espécie do catálogo (sprite animado, nome, uma linha de personalidade em `strings.pets.traits` — as mesmas que a loja mostra), campo de nome com sugestão + 🎲, e o aviso de que todos os outros dá pra adotar depois na loja. É grátis (`adoptStarter`, sem moedas) e já nasce equipado; não dá pra farmar porque Cancelar sessão zera as moedas junto. (3) **Janelas de estudo**, se pula fins de semana, e a **declaração de idade** (`#onb-age`, obrigatória — ver "Os documentos legais"; o botão `#onb-begin` fica travado sem ela) ("← Trocar de pet" volta) — `#onb-windows` (o mesmo `StudyWindowsEditor` das Configurações, com um `id` próprio), `#onb-windows-add`, e `#onb-windows-preview` dizendo ao vivo no que aquilo dá ("Dá 16 pomodoros · 6h 40min de estudo"). **Trocado em 2026-09-10**: o passo pedia duas datas ("período de uso") e nunca perguntava o horário, então toda conta nova herdava 09:00–18:00 — 16 pomodoros e 6h40 num dia — sem ter escolhido, com meta de 60 min. As datas foram pras Configurações → Geral, onde já existe a frase que explica pra que servem; `finishOnboarding` grava `periodStart` = hoje e `periodEnd` = null (o modo "sempre", que era o que quase todo mundo escolhia). Cada passo mostra "Passo N de 3" (`#onb-step`), e o cumprimento "👋 Bem-vindo!" é o **primeiro** passo, não o terceiro. Quem já tem pet vai direto pro último passo. Conta nova (e sessão recomeçada) já nasce com a refeição das 13:00 no plano — a série `ser_almoco`, um evento como outro qualquer (num plano só de noite ela fica fora das janelas, visível e apagável, como qualquer bloqueio). `finishOnboarding` valida tudo antes de mudar qualquer coisa (`empty`, `invalid-window`, `overlap` — de `validateDayWindows` —, `no-starter`, `unknown-species`, `invalid-name`); a aparência entra por `normalizeAvatar`, então id inválido cai no padrão em vez de recusar. e2e **40** e **47**. Config é editável depois em Configurações.
- **Settings é página inteira, não modal** (`#settings-panel.settings-page`, `position:fixed;inset:0`): topbar com "← Voltar" + título, sub-nav de abas sublinhadas ("Geral" / "Estrutura do dia"), área de scroll própria (`.st-scroll`, conteúdo em coluna de 620px centralizada) e barra de ações fixa embaixo ("↺ Padrão" / "Salvar"). É a única tela do app que não é modal — o resto continua em `.panel-overlay.center`. Abre sempre em "Geral", a primeira aba (2026-09-06: Geral passou na frente e a antiga "Rotina" virou "Estrutura do dia"; `switchSettingsTab` também zera o scroll ao trocar de aba). O id interno da aba continua `day` por compatibilidade.
- **Anatomia visual do settings**: cada assunto é uma `.st-section` = título em caps (`.st-section-title`) + frase explicando em linguagem normal (`.st-section-desc`) + um `.st-card` com os controles. Estrutura do dia tem Janelas de estudo / Ritmo do pomodoro / Como fica o dia (por último, como resultado do que está acima); Geral tem Período de uso / Importar calendário (ver "Importar calendário") / Meta diária / Bloqueio de sites (ver "Bloqueio de sites") / Modo hardcore (ver "Modo hardcore") / Aparência / Tutorial ("Ver o tour de novo", ver "Tutorial (tour contextual)") / Meus dados / Zona de perigo. Checkboxes viraram switches (`.st-switch`, o `<input type=checkbox>` continua lá com o mesmo id — só está visualmente escondido). O "Como fica o dia" (`#config-preview`) mostra a linha do tempo da rotina (`DayTimeline`, `#day-timeline`: só janelas e ritmo — refeições e eventos entram pelo Plano) e 4 tiles (pomos / estudo / pausas / XP-dia, o último em accent) mais uma frase `.sts-note` `.ok`/`.warn` dizendo em português o que acontece no fim do dia — não é mais um dump de texto monoespaçado. **A janela fecha no minuto que ela promete** (2026-09-10): numa janela só, sem evento, `diffMins` é sempre 0 e a nota é a `ok`. O aviso laranja continua vivo pro que é sobra de verdade — uma segunda janela curta demais pro pomo, por exemplo. (O `restGap`, que existiu por algumas horas nesse mesmo dia pra não pintar de laranja a sobra da pausa final, saiu junto com a causa: o gerador não produz mais essa sobra.)
- **Botão "🎯 Encaixar estudo"**: na aba Estrutura do dia, dentro do card "Ritmo do pomodoro", abaixo dos três inputs e separado por um `.st-divider`. Abre o modal `#fit-study-panel` (wizard). Usuário diz pomo/pausa curta/pausa longa **ideais** e flexibilidade (±5/10/15min). Algoritmo varia cada parâmetro dentro da flex (pomo passo 5, short passo 1, long passo 5), simula `generateBlocks` pra cada combinação no **dia visível** (respeitando as janelas e os eventos do dia), ranqueia por `studyTotal - penalty*0.5` onde penalty cresce com desvio do ideal (pomo peso 1, short 0.5, long 0.3), e mostra top 3. Clicar "Aplicar" preenche os inputs do form `#cfg-pomo/#cfg-short/#cfg-long` (não salva direto — usuário confirma com "Salvar" no settings, mantendo o fluxo padrão). Sem mexer nas janelas — só nos parâmetros do pomodoro.
- **`periodStart` é fixo por sessão**: na aba Geral, o input "Início" fica `disabled`. Só `periodEnd` e `skipWeekends` são editáveis. `resetSettings` e `clearPeriod` preservam o `periodStart`. Pra mudar o início, o usuário precisa **cancelar a sessão**.
- **Meus dados**: card em Configurações → Geral (acima de "Documentos" e da Zona de perigo), com três coisas.
  - **"Baixar (JSON)"** (`#export-data-btn`): o documento inteiro como é salvo (`serializeState`) mais `exportedAt`, sem uid, e-mail, carimbo de sync nem `safetyNet`, com o nome `study-pets-AAAA-MM-DD.json`. É o direito de portabilidade (art. 20 do GDPR, art. 18 da LGPD) — **não pode sumir**, mesmo que a garantia do backup passe a ser outra.
  - **"Restaurar de um arquivo"** (`#import-data-btn` → `#import-data-file`): o JSON volta a ser o estado. O portão é `readBackup` (`domain/backup.ts`), que recusa com motivo o que não é backup do app — sem ele, `hydrateUserDoc` (tolerante de propósito) devolveria estado **vazio** pra qualquer arquivo, e o save seguinte gravaria isso por cima do histórico. Escolher a foto errada não pode apagar o ano.
  - **A rede de segurança** (`#safety-net`, só quando existe): apagar o histórico deixou de ser definitivo. `cancelSession` tira uma foto do estado inteiro ANTES de zerar e guarda em `state.safetyNet`; por **30 dias** aparece o card com "Restaurar" (`#safety-net-restore`) e "Descartar" (`#safety-net-discard`). A cópia mora no próprio documento — quando o histórico é apagado, o que sobra no doc é quase só ela, então o tamanho não dobra —, nunca contém outra cópia dentro (`safetyNet: null` na foto), e some sozinha no primeiro acesso depois do prazo (`dropExpiredSafetyNet` no `initAfterLoad`; não há tarefa agendada).
  - **A rede NÃO cobre apagar a CONTA**, de propósito: ali a promessa é que não sobra nada, e a política de privacidade diz isso. Guardar uma cópia escondida de quem pediu para ser esquecido quebraria o art. 17. Um teste varre `application/account.ts` atrás de qualquer menção a `safetyNet` e reprova.
  - **Por que existe**: "baixe seus dados antes de apagar" é jogar no usuário uma responsabilidade que é do app — e destoa de um projeto cujo princípio é nunca fazer a pessoa se sentir mal por mudar de ideia. O download continua sendo o direito dela de sair com os dados; a garantia de que nada se perde é nossa.
- **Cancelar sessão** (na tela: **"Apagar todo o histórico"** desde 2026-09-11 — o nome antigo era um eufemismo que soava como logout; o caso de uso continua `cancelSession` e os ids continuam `#cancel-confirm-panel`): botão "Zona de perigo" na aba Geral. Os dois modais da Zona de perigo oferecem **"⬇ Baixar seus dados antes"** (`#cancel-export`, `#del-acc-export`), que chama o `exportMyData` e confirma por toast — o app não tem importação, então é a única rede de segurança. Modal de confirmação lista o que vai apagar. Ao confirmar: zera `checks`, `events`, `eventSeries`, `windowOverrides`, `groups`, `pets`, `coinsSpent`, e reseta `config` pro `DEFAULT_CFG`. Em seguida abre o onboarding (equivalente a uma conta nova). É a única forma de redefinir o `periodStart`.
- **Apagar conta**: segunda linha do card "Zona de perigo" (aba Geral), abaixo de Cancelar sessão e separada por um `.st-divider`. Abre o modal `#delete-account-panel`, que exige o usuário **digitar "APAGAR"** (case-insensitive) pra destravar o botão — proteção contra clique errado, já que o botão de Cancelar sessão fica logo acima. Quando a conta é de e-mail/senha (`state.user.provider === 'password'`), o modal também exige uma **senha** (`#del-acc-password`, campo obrigatório) antes de destravar o botão — é o que a reautenticação usa no lugar do popup do Google. Ao confirmar: `deleteDoc(users/{uid})` **primeiro** (depois do `deleteUser` não sobra credencial pra passar nas rules do Firestore), depois `deleteUser(user)`. Se o Firebase responder `auth/requires-recent-login`, reautentica e tenta de novo — por popup do Google, ou com `reauthenticateWithCredential(user, EmailAuthProvider.credential(email, senha))` quando o provedor é `password`. A flag global `accountDeleted` + `clearTimeout(saveTimeout)` travam o save debounced pendente pra ele não recriar o doc logo após a exclusão; `onAuthStateChanged` zera a flag quando alguém loga. Sucesso → `onAuthStateChanged(null)` devolve a tela de login. Erros aparecem inline em `#del-acc-status` (não é toast — o modal continua aberto pra tentar de novo; mensagem genérica cobre tanto reauth do Google quanto senha errada — ver decisão no plan file). No modo teste (sem Firebase) o fluxo é o mesmo na UI, mas o confirm só zera o state em memória e abre o onboarding — a senha é aceita mas não é de fato checada (não há "login recente" a simular em memória).
- **Janelas do dia, "Começar agora" e "Dia livre"**: as janelas têm override por dia (`windowOverrides[dateKey] = { studyWindows }`, lista vazia = dia livre) — "acordei tarde, hoje começo às 10" não muda mais a rotina nem cria evento falso. Botão "🕘 Janelas do dia" (`#day-windows-btn`) na barra ao lado de "Agrupar" / "+ Evento", só em dia editável (hoje ou futuro, não encerrado); dia com override mostra "🕘 Janelas · editado" em laranja. Abre o modal `#day-windows-panel` com o mesmo `StudyWindowsEditor` das Configurações (só pra este dia), "+ Adicionar", **"▶ Começar agora"** (`#day-windows-start-now`, só hoje: a janela que contém agora — ou a próxima — passa a começar no próximo múltiplo de 5 min; janelas já passadas ficam), **"🌴 Dia livre"** (`#day-windows-off` → confirmação `#day-windows-off-confirm`; só hoje/futuro, e hoje só sem check — declarar depois de falhar seria o "streak freeze") e **"↺ Restaurar rotina"** (`#day-windows-restore`, quando há override). Validação: fim > início, sem sobreposição, lista não vazia (vazio só pelo botão de dia livre). **Dia livre é neutro**, igual ao fim de semana com `skipWeekends`: `allDays()` deixa ele de fora, então não quebra a sequência, não conta como meta batida e tem 0 planejado; nos dots vira `off` e no heatmap `day-off` (apagados). "Prolongar estudos" num dia com override estica o override; "Encaixar estudo" simula com as janelas do dia visível. Cancelar sessão zera. **Fim de semana pausado é um dia livre implícito** (2026-09-06): o botão abre o mesmo modal com a nota "Fim de semana livre" e "+ Adicionar"; salvar janelas ali faz **só daquele sábado** um dia de estudo (`windowOverrides[sáb]`) — o domingo e os outros sábados continuam de folga — e "↺ Restaurar rotina" (ou "Dia livre") devolve a folga. Esse sábado é um **dia bônus** (`isBonusDay`): entra no plano, no XP, nas moedas e na meta como qualquer dia, mas na **sequência só conta se bateu a meta** — se não bateu, não quebra (estudar na folga nunca pode ser pior que não estudar: `calcStreaksNow` filtra o dia, e `computeStats` não zera o `runningStreak` do bônus de moedas num dia `bonus`). Na Análise ele é dia normal: `restDayKind` (`weekend` / `off` / null) alimenta `goalWeek` e `heatmap` no lugar de `skipWeekends` + `dayOff`. e2e **30**. A alternativa "dia livre quebra a sequência" vive na branch `feat/revisao-fundacao-alt-dia-livre-quebra`.
- **Dia vazio**: se `blocksForDay()` retorna `[]` (fim de semana com `skipWeekends`, ou dia declarado livre), a lista mostra "🌴 Fim de semana livre" (pausado) ou "🌴 Dia livre" (declarado), com a dica "toque em 🕘 Janelas do dia" quando o dia é editável (`.empty-day-hint`).
- **Dia futuro**: navegar pra qualquer dia > hoje mostra blocos esmaecidos (`.block-row.day-future`, opacity .45) e clicar dispara toast "Ainda não chegou 🔮" — não inicia timer nem marca check. Helper `isFutureDay(dateKey)`. `toggleCheck` também blinda como fallback. Espelha o padrão `.day-closed`/`isDayClosed` que já existia pra dias encerrados.
- **Modo foco**: ao clicar num bloco de estudo ou pausa **de hoje que ainda não terminou** (passou validação de `tryStartTimer`), abre overlay tela-cheia `#focus-overlay` por cima da UI: chip "Ciclo N · Bloco M", nome do bloco limpo, timer circular SVG que drena (`stroke-dashoffset`), "+X XP · +Y 🪙 ao concluir", e card "Em seguida" com o próximo bloco do dia (ou "Fim do dia 🌙"). Anel verde pra estudo, azul pra pausa. Sem "pular" (decisão consciente: pular é fugir do plano); **"⏸ Pausar" existe desde 2026-09-10** — ver "Pausar o bloco". Botão "← Sair do foco" no topo só fecha o overlay e mantém o timer rodando — pra cancelar mesmo, a `timer-bar` no topo da UI normal tem o "✕ Parar". **Antes da hora** (o bloco começa em 3 min, digamos): o foco abre **em espera** — anel cheio e apagado (`.waiting`), contagem regressiva até o início em cinza, "começa às HH:MM", e a barra diz "Começa em" — e vira o pomodoro normal sozinho quando o relógio chega lá (`timerProgress(...).phase`: `waiting` → `running`; sem estado extra, é só o relógio). **Fim do bloco dentro do foco** é conquista: `checkBlock` marca o bloco sozinho (sem desmarcar um check feito à mão no meio), toca o som `sucesso` (arpejo "deu certo"), e o foco **emenda** no bloco seguinte (`chainedBlockAfter`: estudo → pausa → estudo…, só estudo/pausa que começa exatamente quando este acaba), mostrando a faixa `#focus-done` "✓ Estudo 3 concluído · +50 XP · +25 🪙" por ~4s (`derived.timerCompleted`, escondida pelo relógio, sem timeout). Almoço, evento, gap entre janelas, fim do dia ou dia encerrado param a sequência: marca, fecha o foco e a mesma frase vira toast. **Com o foco fechado** (só a barra), o fim continua como sempre foi: som do tipo, notificação, e o timer some — sem check automático. **Aba em segundo plano / celular travado**: o `setInterval` é estrangulado ou congela, então ao voltar pra visível (`visibilitychange`/`pageshow`) o app chama `reconcileTimer` — o mesmo caminho de fim, um bloco por vez se vários passaram (cada emenda cai no `done` de novo). Enquanto o foco está aberto o **Wake Lock** segura a tela ligada (solta ao sair do foco, parar ou terminar; re-pede ao voltar pra visível, porque o browser solta sozinho ao esconder a página). Espaço `.focus-scene-stage` está reservado pra cena ambiente (personagem + pet), por enquanto vazio.
- **Pausar o bloco** (2026-09-10): "⏸ Pausar" na barra (`#timer-pause`) e no foco (`#focus-pause`), só com o bloco **rodando** (em espera não há o que congelar) e **fora do hardcore** (o modo é o compromisso — o botão some). A vida interrompe; pular continua não existindo. **Pausado**: o relógio congela (`timerProgress(block, now, derived.timerPausedAt)` → `phase: 'paused'`, restante = fim − instante da pausa, rótulo "Pausado · 03:12"), o bloco não termina mesmo passando do fim, a tela pode travar (Wake Lock solto), e a extensão **continua bloqueando** com o fim deslizando ao minuto (se pausar liberasse, pausar seria o desbloqueio de um clique — quem quer navegar tem o "✕ Parar"). **"▶ Retomar"** (`resumeTimer`): a pausa vira um `PauseRecord { at, mins }` em `state.pauses[hoje]` (`at` = o minuto em que pausou, pra baixo; `mins` pra cima, mínimo 1 — quem pausou nunca perde segundos), o dia é regenerado (`generateBlocks(cfg, events, pauses)`): o bloco que contém `at` fica `mins` mais longo — **um bloco só**, mesma chave de check, mesmo "Estudo N", `endTime` esticado e `paused: mins` — e tudo depois dele desliza; eventos e o fim da janela **não se movem**, então o bloco empurrado contra eles é cortado (o último estudo do dia encolhe, ou some; uma pausa que atravessa o evento se dissolve nele). A duração que vale é `blockMins`: XP, moedas, meta, aderência, grupos e o anel descontam a pausa, e um dia com pausa vale o mesmo que sem (teste). O timer segue no bloco **regenerado** (o snapshot antigo não seria mais achado por `chainedBlockAfter`) e emenda no seguinte no horário novo. Checks feitos adiantado e grupos acompanham os blocos que deslizaram (`pauseRemap`, por posição na cadeia contígua a partir do bloco pausado; o que vem depois de um evento não se move); check de bloco que sumiu é descartado, e o toast conta ("⏸ Pausa de 7 min · o dia anda 7 min · último estudo com 18 min"). Retomar com o bloco já terminado (a pausa atravessou o fim) **para sem check nem som** — marcar algo que a pessoa não viu acabar seria desonesto. O save é imediato (`saveNow`). **A pausa aberta fica no dispositivo** (`infrastructure/pauseSession.ts`, `localStorage` por uid, como a sessão hardcore): recarregar volta o timer **pausado na barra** (foco fechado) com Retomar/Parar (`resumePauseOnBoot`, depois do hardcore — se o hardcore retomou a sessão dele, a pausa é esquecida); de outro dia, esquecida. Meia-noite pausado → timer encerrado, nada registrado. "✕ Parar" no meio da pausa → nada registrado (registro é só de bloco que continuou). Encerrar o dia com o timer pausado para o timer; o prompt de fim de dia não abre enquanto pausado. **Histórico**: a linha do bloco mostra `⏸ 7 min` (`.block-paused`, entre o nome e o XP) — é o que fecha a conta na cabeça de quem vê "09:00–09:32 · +50 XP"; o dia passado é regenerado com as pausas dele; o resumo do fim do dia mostra "⏸ 2 pausas · 11 min" (`#ds-pauses`). Nada na Análise no v1 (seria o app te olhando torto). Cancelar sessão zera. Ids/classes: `#timer-pause`, `#focus-pause`, `.timer-time.paused`, `.focus-overlay.paused`, `.focus-time-big.paused`, `.block-paused`. e2e **38**.
- **Modo hardcore**: dificuldade **escolhida**, nunca padrão (a base do app continua sendo "nunca fazer o usuário se sentir mal" — aqui a pressão é dele sobre ele mesmo, e o app só cobra o que ele combinou). Liga em Configurações → Geral → "Modo hardcore" (`config.hardcore = { enabled }` — só isso; `#cfg-hardcore`, mais a linha `#hardcore-site-note` dizendo que bloquear site é outra seção). **Hardcore é só a penalidade** desde 2026-09-08: a lista de sites saiu daqui e virou "Bloqueio de sites" (abaixo), que funciona com ou sem ele. Ligado, **tocar num estudo/pausa abre o consentimento** `#hardcore-start-confirm` com o custo escrito ("Sair antes do fim custa −100 XP pra você e −100 XP pro Bolt"), "Começar" (`#hardcore-start-btn`), "Cancelar" e "Só desta vez, sem hardcore" (`#hardcore-start-normal`, o foco normal). No foco hardcore (`.focus-overlay.hardcore`, chip `#focus-hardcore`) **não existe "Sair do foco" nem "Parar"** (`closeFocus`/`stopTimer` viram no-op): a porta é **"Desistir…"** (`#hardcore-quit` → `#hardcore-quit-confirm` com a conta exata → `#hardcore-quit-btn`); numa **pausa** é "Parar aqui (sem custo)" (`#hardcore-stop`) e **em espera** (aberto antes da hora) é "Cancelar (sem custo)" (`#hardcore-cancel`) — a pausa é a saída natural, o compromisso é com o estudo. **A conta**: desistir de um estudo custa **2× o XP do bloco** (pomo de 25 min = −100), pro usuário **e** pro pet equipado, **na hora** (não espera o dia fechar — é a única exceção ao "XP só de dia fechado"), limitada ao que cada um tem (nunca negativo); **nível pode cair**, dos dois; a **forma do pet fica** (evolução é definitiva — um Lobo no Lv. 4 continua Lobo, sem vantagem nenhuma: o bônus da skill é por nível e o avanço do Lv. 15 continua trancado). Moedas não mudam. O bloco abandonado **perde o check e fica trancado** (`.block-row.forfeited`, ✕ no lugar do check, "desistiu"; `canCheckBlock` e `canStartBlock` recusam com `forfeited`) — senão desistir seria de graça. Registro em `state.penalties[dateKey]` (`PenaltyRecord`: bloco, `xp`/`petXp` de fato descontados, pet, `reason: quit | abandon`), agregado na mesma passada do `computeStats` (`penaltyXP`, `quits`; `totalXP` desconta no fim, com `max(0, …)` de cinto). **A sessão vale quando o bloco começa a rodar** (`derived.hardcore.armed`): aí ela vai pro `localStorage` deste dispositivo (`infrastructure/hardcoreSession.ts`, por uid — não vai pro doc de propósito), e o `beforeunload` é armado ("certeza que quer sair?", texto do navegador); a emenda estudo → pausa → estudo atualiza a sessão (pausa: sem guard). Quem avisa a extensão é o bloqueio de sites, não isto. **Ao abrir o app** (`resumeHardcoreOnBoot`, em `initAfterLoad`): sessão com o bloco ainda rodando → o foco volta em hardcore (recarregar não é sair); estudo que acabou sem o app → **abandono**, a mesma conta cobrada agora, uma vez (toast "O app fechou no meio de…"); pausa que acabou → esquece. Trocar de aba **não** pune (v1 consciente; o bloqueio de sites cuida das distrações). Com o bloqueio ligado, o consentimento ganha a linha `#hardcore-start-sites` ("🛡️ chess.com fica bloqueado até o fim", ou o aviso de extensão ausente) e a tela do pet diz "desista lá no app — custa XP". Celular: cobra XP, não bloqueia. Cancelar sessão zera `penalties` (e a config volta desligada). Ids/classes acima são contrato com o e2e **28/29**.

- **Bloqueio de sites**: feature própria desde 2026-09-08 (`config.siteBlock = { enabled, mode, sites }`), **independente do hardcore** — o Tomi pediu "dar a opção de bloquear sites sem modo hardcore", e que uma entrada cobrisse o site inteiro.
  - **Quando bloqueia**: enquanto um **estudo** está rodando no timer do app — foco aberto ou só a barra, com ou sem hardcore. Começa quando o bloco entra em `running` (em espera não), **continua com o timer pausado** (o `until` desliza ao minuto — ver "Pausar o bloco"), segue a emenda estudo → pausa → estudo (a pausa libera, o estudo seguinte bloqueia de novo) e acaba no fim natural, no "✕ Parar", no "Desistir" do hardcore, no fim do dia e no logout. Quem decide e publica é **`application/siteBlock.ts`, fonte única** — o hardcore não fala mais com a extensão.
  - **`stopped` x `unknown`** (a decisão que impede o escape): o timer do app é runtime e não sobrevive a um reload. Se ao carregar a página o app dissesse "inativo", recarregar liberaria o site no meio do estudo. Então o app só manda `stopped` quando **ele** encerrou, e só se aquela carga da página armou algo antes; à pergunta da extensão sem nada rodando, responde **`unknown`**, e a extensão mantém o que tinha até o alarme do `until` vencer — um pomodoro de 25 min nunca deixa regra pendurada por mais que isso.
  - **O que uma entrada cobre**: `chess.com` pega `chess.com`, todo subdomínio e qualquer caminho ou porta (é o `requestDomains` do declarativeNetRequest). Dá pra colar a URL inteira; o que não vira domínio (`chess`, `lol`) **não some em silêncio** — aparece em `#siteblock-invalid` ("Não entendi: chess"). **Apelidos** (`SITE_ALIASES`, tabela pequena e explícita): `youtube.com`→`youtu.be`, `twitter.com`↔`x.com` (+`t.co`), `facebook.com`→`fb.com`/`fb.watch`, `instagram.com`→`instagr.am`, `reddit.com`→`redd.it`. É a lista **expandida** que vai pra extensão — ela continua burra. Só `main_frame` (bloquear abrir o YouTube, não quebrar um site que embute vídeo). "Permitir só estes" continua, com `ALWAYS_ALLOWED` (o app, localhost, login do Google/Firebase) sempre livre.
  - **A UI** (`features/settings/SiteBlockSection.tsx`; Configurações → Geral, antes do Modo hardcore): switch `#cfg-siteblock`, chips de modo `#cfg-siteblock-mode` (`data-mode`), textarea `#cfg-siteblock-sites`, os chips do que foi entendido em `#siteblock-preview` (`.sb-chip`, apelido entre parênteses), os inválidos em `#siteblock-invalid`, e `#siteblock-ext-status` em **três estados**: não encontrada (com o guia de 3 passos e "clique em ↻"), encontrada (com a versão que o content script anuncia) e **bloqueando agora** ("✓ Bloqueando 3 sites até 02:55", vindo do ack). Botão **"▶ Testar por 1 min"** (`#siteblock-test`, só com extensão): publica a lista **do rascunho** por 60 s (`test: true`) — dá pra testar antes de Salvar; vira "■ Parar teste". Enquanto a extensão confirma, a `timer-bar` e o foco mostram "🛡️ 3 sites bloqueados" (`#timer-site-block`, `#focus-site-block`).
  - **A extensão** (`extension/`, v0.2.0, só neste navegador): eventos `study-pets:blocking` (app → ext), `study-pets:blocking?` (ext → app) e `study-pets:blocking-ack` (ext → app), payload **`v: 2`** com `{ active, until, mode, sites (expandida), block, pet, appUrl, hardcore, test }` | `{ active: false, reason: 'stopped' | 'unknown' }`; `v: 1` é ignorado. O atributo de detecção no `<html>` é a **versão** do manifest. O **ack** (`{ applied, until, sites, mode, test }`) volta pro app e vira `derived.siteBlock.ack` (runtime, não persiste). A extensão tem badge "ON", popup (o que está bloqueado, até quando, "Abrir Study Pets"), redireciona abas já abertas ao armar e vigia `tabs.onUpdated` (voltar pelo histórico, bfcache, prerender). As regras somem sozinhas no fim do bloco (alarme), mesmo com a aba do app fechada.
  - **Prova**: `npm run test:ext` (`e2e/extension.spec.ts`) roda num Chromium de verdade com a extensão carregada — o site cai na tela do pet, a badge acende, recarregar o app não libera. Smoke **33** cobre a UI e o evento publicado, sem extensão. Cancelar sessão zera a config.
- **Grupos de estudo**: nome + objetivo sobre um trecho do dia ("Análise II · terminar a lista 3"), criados direto no plano. Três portas de entrada pro mesmo estado de seleção (`useGroupSelection`): arrastar com o **botão direito** no desktop, **segurar o dedo** numa linha no celular e arrastar (soltar sem arrastar cai no modo de tocar no último bloco), ou o botão **"Agrupar"** ao lado de "+ Evento" (toca no primeiro bloco, depois no último; com mouse, passar por cima mostra o intervalo). Durante a seleção um retângulo tracejado envolve as linhas escolhidas; no celular, o toque longo segura o scroll com um `touchmove` nativo não passivo e captura o ponteiro na linha, e perto da borda da tela a página rola sozinha. O intervalo vira o modal `#group-panel` (nome, objetivo opcional; Enter salva). Grupo é **anotação por horário** — `{id, start, end, name, goal}` em `state.groups[dateKey]` — e **nunca entra no `generateBlocks`**: pertencimento se calcula na render (bloco que cabe inteiro no intervalo), então config/evento mudando não corrompe nada, igual aos checks por horário. Na lista, o grupo é uma **caixa** (`.group-box`, `features/groups/GroupBox.tsx`) que envolve o cabeçalho `.group-header` (nome, objetivo, `feitos/total`, minutos e barra de progresso) e as linhas membros (`.in-group`; só estudo e evento contam, pausa é membro visual). Membros são contíguos porque blocos são sequenciais e grupos não se sobrepõem; divisor de ciclo que coincide com o início do grupo fica fora da caixa. **Cores dos grupos** (`gc-0`…`gc-5` no CSS, pela ordem do grupo no dia): paleta fria e luminosa (ciano, índigo, turquesa, magenta, céu, lilás), de propósito fora da paleta das sessões — grupo é outra camada. Completo vira verde. Tocar no cabeçalho edita/apaga. **Ajustar o trecho**: puxando a alça (`.gb-grip`) na borda de cima ou de baixo da caixa, com mouse ou dedo (o retângulo acompanha; a alça de baixo não passa da linha de cima, e vice-versa; soltar chama `updateGroup`, e sobreposição vira toast sem mudar nada). Digitar início/fim no painel foi testado e **removido**: horário livre corta bloco pelo meio, e trecho é sempre de linhas inteiras — o painel só mostra o resumo. O menu de contexto do browser é bloqueado durante arrasto/toque longo por um listener de captura no `document` (o modal abre no pointerup, fora da lista). Validação em `domain/groups.ts`: fim > início, sem sobrepor outro grupo, ≥ 1 estudo/evento dentro. Dia encerrado é read-only; dia **futuro pode** (planejar é o ponto). Cancelar sessão zera. v1 consciente: sem recorrência, sem "matéria"/cores, sem "cumpri o objetivo?" — ver IDEIAS.md.
- **Tutorial (tour contextual)**: balões em cima do app de verdade, apontando pro elemento real, na **primeira visita a cada aba** — não são cartões antes da primeira tela (decisão em IDEIAS.md, "Tutorial depois de criar a conta"). Cinco balões, tom adulto, explicam o **modelo** e não os botões: Plano (3) — "Seu dia já está montado" (âncora: a barra do dia `.day-events-bar`, anel na primeira `.block-row`), "A vida muda, o plano acompanha" (`#add-event-btn`), "No fim do dia, encerre" (`.finish-day-btn`; se o botão não existe — outro dia visível, dia encerrado — vira um cartão fixo no rodapé, sem seta); Perfil (1) — "Pets são horas estudadas" (`#active-pet-card` ou `#no-active-pet`); Análise (1) — "Tô fazendo o que planejei?" (`#an-subnav`). Textos em `strings.tutorial.steps`, duas linhas. **Gatilho**: `activeTourArea(tutorialSeen, uiTab, {onboardingOpen, loaded})` — só com o onboarding fechado, o app carregado e a aba visível ainda não vista; Plano é o primeiro porque é a aba inicial. Conta que já existe (doc sem `tutorialSeen`) também vê o tour uma vez. **Não bloqueia a tela**: sem backdrop, `pointer-events` só no balão, `z-index` 80 — acima da lista, abaixo do ⚙️ (90), da barra do timer (99), do topbar (100), dos modais/Configurações (200) e do foco (300), então nunca cobre um modal. Posição em coordenadas do documento (rola junto com o elemento), medida a cada render e em resize/relayout (`ResizeObserver` no body); `placeBalloon` (domínio) decide o lado e clampa na tela.

**A conta é nos dois eixos** (2026-09-10): `placeBalloon` recebe a **faixa visível** (`TourViewport`: largura, e o topo/base em coordenadas do documento), não só a largura. O topo da faixa já vem descontado do que a barra fixa cobre — a `.topbar` é sticky com `z-index` 100 em qualquer largura, e a `.timer-bar` gruda abaixo dela enquanto um bloco roda (`topInset()` no `TourBalloon` mede as duas). Sem isso, "cabe acima do elemento" era mentira sempre que o espaço acima ficava debaixo da barra: no layout B7 a barra do dia é sticky logo abaixo da `.topbar`, e o balão 1/3 nascia com 25px escondidos atrás dela. Agora ele **vira pra baixo** no laptop — cobre as primeiras linhas da lista, que é o preço de não ter espaço acima de um elemento grudado no topo; **em 480px nada muda** (acima ainda cabe, o balão fica onde sempre esteve). Não cabendo de lado nenhum, fica no lado preferido encostado na borda: balão deslocado se lê, balão fora da tela não. E `scrollToShow` **traz a âncora pra vista antes de medir** (rolagem instantânea, num `useLayoutEffect` declarado antes do que posiciona — o lado é decidido contra a faixa visível, então a página tem que já estar no lugar): o balão 3/3 aponta pro "Encerrar o dia", no fim da lista, e nascia 440px abaixo da dobra no laptop e 865px no celular — clicar "Próximo" fazia o tour parecer sumir. Elemento já visível não faz a página pular. **Posição é contrato com o smoke test**: o balão do Plano fica **acima da barra do dia** (Janelas do dia / Agrupar / + Evento), alinhado à esquerda, com 280px — a barra inteira, os checks, os nomes dos blocos e o cabeçalho de um grupo ficam livres, e ele cobre só o seletor de semana e as abas dos dias (acima da lista, ele cobria o botão mais à esquerda da barra); o do Perfil fica acima do card do pet (os botões "Meus pets"/"Loja" ficam livres). Mudar isso = rodar o e2e. "Pular" (sempre visível) e "Entendi" no último balão marcam a **área inteira** como vista (`finishTour`); avançar entre balões é `useState` do componente — trocar de aba e voltar recomeça a área do primeiro balão (não persiste o passo). "Ver o tour de novo" (Configurações → Geral → Tutorial, `#tour-restart`) zera `tutorialSeen` e fecha as Configurações — o ⚙️ só existe no Plano, então o balão 1/3 aparece embaixo. **Cancelar sessão não zera** (quem cancelou já conhece o app). Ids/classes: `#tour-balloon` (`.tour-above`/`.tour-below`/`.tour-card`, `data-step`), `.tour-ring`, `.tour-arrow`, `#tour-next`, `#tour-skip`, `#tour-restart`, `#add-event-btn`. e2e **27** (celular) e **45/46** (laptop, 1280px): o helper `balaoUtilizavel` mede o retângulo **dentro da página** e cobra janela e barra do topo — `toBeVisible()` do Playwright passa com o elemento atrás de uma barra fixa ou abaixo da dobra, que foi como os dois bugs acima viveram verdes na suíte.

- **Notificações (o sininho)** (2026-09-11): o painel ao lado da engrenagem com **o que aconteceu enquanto você não estava olhando**. A `Aba de notificações` do IDEIAS.md, decidida: sininho (não uma quarta aba), histórico (não "lida e some"), e uma régua estreita pro que entra.
  - **A régua**: toast é resposta a uma ação que a pessoa acabou de fazer, e some porque ela estava olhando. O sininho é pra quando ela **não** estava. Na prática isso é a **regra do dia fechado** (XP do usuário e do pet só entram quando o dia entra na conta) mais o **boot assíncrono** — os dois únicos momentos em que o app descobre algo sozinho.
  - **Um dia entra na conta por três portas, e as três passam por `applyPendingPetXP`**: o botão "Encerrar o dia" (que o chama logo depois de fechar), a **virada da meia-noite** descoberta no boot seguinte — `computeStats` conta todo dia passado, com ou sem `closedDays` — e a virada **com o app aberto** (`application/dayRollover.ts`: um `setTimeout` pro instante da virada mais o `onVisible`, sem polling; sem ele, quem deixa o app aberto na aba Plano atravessando a meia-noite ficava sem o crédito do pet e sem linha nenhuma até recarregar). A segunda é a comum (a pessoa fecha o laptop) e era a que **não tinha tela nenhuma contando**: o pet subia de nível de madrugada e ninguém via. Por isso o gatilho das sete primeiras é **um só**, dentro do `applyPendingPetXP`: a janela `(pending.from, pending.processedUntil]`, com os números por dia vindo de `stats.dayXP`/`stats.dayCoins` (campos novos do `computeStats`, acumulados na mesma passada). Duas fontes calculando os mesmos ids com números possivelmente diferentes seria pior que um caminho só. Voltar de muitos dias fora rende no máximo **três** linhas de "dia encerrado" (`MAX_DIAS_NO_LOTE`), mas os marcos do período inteiro entram — e dois níveis num lote viram **uma** linha, do nível alcançado.
  - **As sete**: `dia` ("Dia encerrado" · "02/09 · +390 XP · 3h15 · 🏆 melhor dia" — em **tempo**, não em moedas: é o que tira a linha de ser cópia do resumo que a pessoa acabou de fechar, e o recorde é uma **marca** aqui, não uma linha própria), `nivel` (você subiu), `pet-nivel`, `pet-evolucao` (destravou agora — o selo "✨ Pode evoluir" do Perfil cobre o **estado**, isto cobre o **instante**), `sequencia` (marco dos degraus de `DAILY_BONUS_TIERS`, sem o de 1 dia), `horas` (`MARCOS_DE_HORAS` = 10/25/50/100/200/500/1000 h — a única linha que fala do total, e a que mais casa com o que o app diz de si mesmo) e `abandono` (o modo hardcore cobrando no boot, hoje um toast que dispara por cima de quem está abrindo o app).
  - **`moedas` ("dá pra adotar mais um pet") foi implementada e retirada**: é a única linha cuja função é fazer você gastar, dentro de um painel que existe pra mostrar o que você perdeu — e ela volta a cada vez que o saldo cruza o preço do pet mais barato, ou seja, depois de cada adoção. A loja já se destrava sozinha e o saldo já aparece em dois lugares.
  - **O que NÃO entra, e por quê**: nada que já é dito em outro lugar (plano reajustado, importação de calendário, dia passou do horário — o prompt já é isso) e **nada que cobra o usuário**: "você não estudou hoje", "sua sequência acabou", "faz N dias que você não aparece", pet com fome. O catálogo inteiro, com o veredito de cada linha, está em `plans/2026-09-11_0130_notificacoes-sininho.md`.
  - **O id é a chave de dedup, e deriva do acontecimento, nunca do relógio** (`dia:2026-09-11`, `pet-nivel:dog:5`, `sequencia:<dia>:<n>`). `addNotifications` é no-op em id repetido e **a linha antiga vence** (o `read` e o carimbo dela ficam, senão abrir o app de novo faria a linha de ontem voltar a brilhar). É o que permite emitir o mesmo lote no fim do dia, num boot depois e num sync de outro dispositivo sem gerar cópia — e o dispositivo que não presenciou o fato recebe a linha pelo documento.
  - **Persistida** (`notifications` no doc; `schemaVersion` continua 4 — campo novo, opcional, default `[]` em `hydrateUserDoc`), teto de 40. O `read` precisa sobreviver ao reload, senão o selo acende a cada abertura. Sync: a lista é substituída inteira, como tudo; ids iguais não duplicam, e o que pode oscilar é o `read`. **Cancelar sessão zera** (as linhas falam de um nível, um pet e dias que acabaram de deixar de existir).
  - **O texto não mora na notificação**: ela guarda os números (`data`), e `strings.notifications` monta a frase — uma linha lida em novembro continua certa depois de o texto mudar em dezembro. Os três mapas de texto são `Record<string, …>`, então um tipo novo sem frase renderizaria uma linha vazia sem quebrar o build: **um teste varre `NOTIF_KINDS` contra eles**, e cobra que nenhum texto escreva "undefined" com dados pela metade.
  - **Onde fica**: no laptop, na `.topbar-right` entre o XP e a engrenagem (a ordem `today-label · xp-badge · notif-wrap · gear-btn · avatar-wrap` é contrato do e2e). **No celular, também no cabeçalho**, entre o XP e o "Sair" — a engrenagem lá é um FAB que só existe na aba Plano, então pendurar o sininho nela o faria sumir na Análise e no Perfil; e o painel vira uma folha fixa logo abaixo da barra, de beirada a beirada, porque ancorado à direita numa barra estreita ele sangraria pela esquerda.
  - **O selo é da cor do accent, não vermelho** — aqui não chega nada urgente nem nada que cobre você; passa de 9 vira "9+". **Marcar como lido acontece ao FECHAR** o painel, não ao abrir: é fechando que dá pra ver, com ele aberto, o que era novo. Estado vazio com duas frases, porque conta nova abre num painel vazio.
  - Ids/classes: `#notif-btn`, `#notif-badge`, `#notif-panel`, `#notif-list`, `#notif-empty`, `#notif-clear`, `.notif-wrap`, `.notif-item` (`.unread`, `.clickable`, `data-kind`). e2e **48** (celular), **49** (laptop), **50** (393px: a barra do timer abaixo de uma barra do topo de duas linhas) e **51** (o caso principal: virar o dia com o app fechado).

## Tela grande (laptop)

**Layout B7 (2026-09-07, branch `feat/tema-cafe`)** — escolhido pelo Tomi num canvas de mockups depois de descartar a versão só-de-tema. A partir de 1100px o `#app` é um grid de **duas colunas**: à esquerda uma coluna de `calc(240 * var(--s) * var(--col-k))` e à direita a página, com `calc(36 * var(--s))` de vão e `calc(48 * var(--s))` de beira. O `--col-k` é a escala da coluna: `features/plan/useColumnFit.ts` mede a altura natural dela (o `#plan-side` mais o cartão do timer) contra a altura livre da janela e escreve a razão no `#app`, entre 0,5 e 1; o CSS usa o mesmo fator no `zoom` da `.plan-col` **e** na faixa do grid, então ela encolhe inteira, com as proporções, e a lista fica com a largura que sobra. Em cima, a **barra do pacote "Café de casa"** (`.topbar.topbar-wide`, `app/Header.tsx`): a marca "study pets." com a pata, as abas como texto de 12px no meio (ativa em negrito na cor do accent), e à direita o XP com o nível, a **engrenagem** (`#gear-btn`, que chama `requestSettings`) e o **avatar** (`#avatar-btn`, a inicial do usuário), que abre o menu `#avatar-menu` com Configurações e Sair (`#avatar-sair`) — clicar fora ou Esc fecha. Medidas exatas do pacote: padding 19×28, marca 20px, XP 11px, avatar 30px. A coluna da esquerda, só no Plano: o `PlanSidebar` (Hoje pendente; Meta diária com a barra e "N de 7 dias" — sem os sete dots) e o **quarto ilustrado** (`.room-card`: janela, mesa, planta e caneca em CSS puro com tokens `--room-*`, o personagem e o pet equipado animados, nome + "Lv. N" + "Ver meus pets"), e por último a `.timer-bar`, que sem timer vira o cartão **"Agora · Iniciar"** (`.timer-bar.idle`, `features/timer/TimerBar.tsx`: o estudo/pausa de hoje em andamento ou o próximo, com o botão `#agora-start`, que **pede** o início pelo store — `requestStartBlock` põe o bloco em `derived.startRequest` e o `PlanTab` atende pelo mesmo caminho do clique na linha, então com o hardcore ligado o botão continua ali e o consentimento abre antes) e com timer é o cartão de sempre. Esse cartão fica por último porque **acompanha o scroll**: quem é `position:sticky` é a coluna inteira (`.plan-col`, `#plan-col` em `app/App.tsx`), logo abaixo da barra e com a altura da janela — o `PlanSidebar` rola por dentro (`.plan-side`, sem barra) e o cartão fica preso no pé. As ações do dia (Janelas do dia · Agrupar · + Evento) são botões de contorno, o Evento cheio no accent. Análise e Perfil ocupam as duas colunas, centrados em `calc(980 * var(--s))`. **Abaixo de 1100px nada muda.** O texto abaixo descreve o layout anterior (trilho · página · coluna da direita, 15/66/15) e vale como história e pra Semana/lista, que continuam iguais.


A partir de **1100px de janela** (`@media (min-width:1100px)` em `app.css` + `useWide()` em `shared/useWide.ts` — a mesma largura nos dois), o `#app` vira um **grid de três colunas centralizado** (**15% · 66% · 15%** da tela, vãos de 1%, margem de 1,5% à esquerda e 0,5% à direita, e a coluna da direita com 24 px de referência de recuo — proporção escolhida pelo Tomi, tiquinho a tiquinho): **tudo do laptop é proporcional à largura da janela**. Os tamanhos (fonte, altura de linha, espaço, sprite) são medidos em `--s`, "um pixel do desenho de referência" (janela de 1480px: `--s: calc(100vw / 1480)`), então a página é a mesma em qualquer largura, só maior ou menor — sem margem morta e sem parte miúda (o Tomi pediu isso depois de ver tetos em pixel distorcerem a cada largura). Análise e Perfil escalam inteiras com `zoom: calc(100vw / 1480px)` (o Plano é medido em `--s` regra a regra porque tem coordenadas de ponteiro: seleção de grupo, alças, balão do tour). A Semana ocupa a altura da janela: o **trilho** à esquerda (o `.topbar` em coluna, sticky: data, abas como texto com um traço na ativa, XP com a barra do próximo nível, o personagem com o pet equipado no pé, e Sair), a **página** no meio e, só no Plano, a **coluna da direita** (a barra do timer vira um card, e embaixo dela o `PlanSidebar`: XP pendente de hoje e a meta diária com os dots da semana). É o esboço de 2026-09-06 (`plans/2026-09-06_2010_cabecalho-e-laptop.md`), com as decisões do Tomi: **largura decide, não o aparelho**; **abaixo de 1100px nada muda** — o celular continua exatamente como era, e os pedaços da tela grande nem são montados (`useWide`), então o smoke test em 480px não os vê; **poucas caixas** — só o toggle e o timer; o resto é texto, hairline e espaço.

- **A lista sem caixa**: no laptop as `.block-row` perdem fundo e borda (hairline embaixo, ciclo na cor do horário, só o bloco de agora com fundo e um traço verde na margem, grupo como traço na cor dele). É só CSS dentro da media query; no celular a linha continua a caixa colorida. O XP card e os três números (`.xp-card`, `.stats-row`) somem — o que importa deles mora na barra do topo (XP/nível) e na coluna da esquerda (o Tomi tirou "Estudos" e "Sequência" de propósito).
- **O ⚙️ flutuante era pra continuar no laptop**, mas o B7 escondeu ele: `.fab-config{display:none}` dentro da media query. Acima de 1100px a engrenagem mora na barra do topo (`#gear-btn`, e Configurações também no menu do avatar); `#fab-config` só existe no celular.
- **Dia · Semana** (`#view-mode`, `#view-day`, `#view-week`, estado local do `PlanTab`; abaixo de 1100px é sempre Dia): a **Semana** (`WeekView`, `#week-view`) é o modo de planejar — um dia por coluna, da primeira à última hora com bloco na semana, cada bloco como mancha de cor na altura da duração (ciclo nas cores de sempre, pausa longa azul, evento laranja, intervalo cinza tracejado, feito = cor cheia), hoje com a linha do agora; fim de semana pausado e dia livre viram coluna vazia. Clicar num dia abre o Dia dele. Ela só lê `blocksForDay` — nunca gera plano.
- **Semana no celular**: em aberto (a proposta de uma linha por dia com faixa de horas não convenceu — ver o plano).
- Ids/classes: `#plan-col` (`.plan-col`), `#plan-side` (`.side-item/.side-k/.side-v/.side-sub`), `#room-card` (`.room-*`), `.timer-bar.idle` (`.agora-k`/`.agora-dur`/`#agora-start`), `.view-mode/.vm-btn` (`#view-day`/`#view-week`), `.day-num`, `#week-view` (`.wv-*`); na barra, `.topbar-wide`, `#gear-btn`, `#avatar-btn`, `#avatar-menu`, `#avatar-sair`. As do trilho (`.rail-xp`, `.rail-scene`, `.rail-foot`) e os dots da coluna (`.side-dot`) saíram no B7 — ficam no histórico do git.

## Aba Análise

A aba responde "tô fazendo o que planejei?" — diagnóstico, não vitrine. Estrutura: **profile card + sparkline sempre visíveis no topo**, depois **sub-nav com 4 chips** (Hoje / Semana / Geral / Recordes; default = Hoje) que troca o conteúdo abaixo via `.subview.active`.

**Profile card (sempre visível)**: nível, XP, barra de progresso pro próximo nível, e abaixo uma **sparkline SVG** com as últimas 8 semanas de minutos de estudo (`stats.dayStudyDoneMins` agregado por `mondayOf(d)`). Prefixa com zeros se houver menos de 8 semanas de dados. Ponto destacado no último valor.

**Sub-abas e o que cada uma mostra:**

- **Hoje** (default): card **grande** "Realizado hoje" (min cumpridos / planejados + barra + %) + card "Meta diária" com 7 dots da semana, com o dia de hoje destacado por anel (`.goal-dot.today`).
- **Semana**: card "Realizado na semana" + card "Meta diária" (dots sem anel) + "Conclusão por ciclo" (drop-off, marcado como *histórico*).
- **Geral**: card "Realizado no geral" (agrega **todos os dias com dados** até hoje, não só o mês corrente) + heatmap GH-style (7×16) + "Horários onde mais estuda" (hour chart).
- **Recordes**: stats row (Blocos / Dias seguidos / Melhor semana) + cartão de recordes (sequência atual, maior sequência, melhor dia, XP num dia).

**Realizado (cumprido vs META, por tempo)**: cada card mostra `min cumpridos / min de meta` (`adherence`, em `domain/analytics.ts`), sub com o contexto (`Xh de estudo · Yh no plano`), barra, e %. Classes `.met` (bateu, com ✓ no %), `.low` (laranja, pct<20) e `.zero` ("nenhum dia cobra meta aqui"). Card "Hoje" tem variante `.adh-big`. **A régua é a meta, não o plano cheio** (2026-09-10): medindo contra o plano, um dia padrão de 6h40 fazia bater a meta de 60 min aparecer como "75 / 385 min · 19%" em laranja — na mesma tela em que o dot da meta ficava verde com "75 min ✓". Duas réguas discordando sobre o mesmo dia, uma em cima da outra. A meta do período é a diária × os dias que **contam e já chegaram** (folga e dia anterior ao `periodStart` ficam de fora); estudar em dia de folga entra no numerador e não infla o denominador — na folga o extra ajuda, nunca cobra. O total planejado continua no card, como contexto. Decisão de granularidade por tempo (não por sessão) é consciente: mais reliable.

**Meta diária — 7 dots**: card com headline "Você bateu a meta de X min em **N de 7** dias esta semana" + 7 spans (Seg–Dom) com classes `.met` / `.miss` / `.future` / `.weekend` / `.before` (antes do início da conta — neutro) / `.today` (anel verde só no card da aba Hoje). Conecta `config.dailyStudyMin` à análise. Se `skipWeekends=true`, headline vira "N de 5" e sáb/dom ficam neutros.

**Heatmap GitHub-style**: 7 linhas (dias) × 16 colunas (semanas) com `grid-auto-flow:column`. Cor por **% da meta diária batida** (0/25/50/75/100+). Cells futuras dashed, sáb/dom (se skipWeekends) neutros. Tooltip "DD/MM (hoje): X de Y min (Z%)". Se `dailyStudyMin===0`, intensidade = 4 se done>0 senão 0.

**Drop-off por ciclo**: linha por número de ciclo (Ciclo 1, 2, 3...) com barra + pct + "done/total". Itera `stats.cycleStats` (só dias fechados). `.do-fill.low` se pct<50.

**Implementação**: `renderAnalytics()` é orquestrador curto que chama todos os sub-renderers (`renderAnalyticsProfile`, `renderAdherenceCards`, `renderGoalWeekDots`, `renderAnalyticsStats`, `renderHeatmapGH`, `renderHourChart`, `renderDropoffChart`, `renderSparkline`, `setupAnalyticsSubnav`). `renderGoalWeekDots` chama `renderGoalWeekDotsTo` 2 vezes (uma pro card da Hoje com `highlightToday:true`, outra pro card da Semana). `setupAnalyticsSubnav` registra um único listener de click em `#an-subnav` que toggla `.active` nos chips e nas subviews — protegido por flag `_anSubnavBound` pra não dobrar se a aba for re-aberta. Os ids dos cards de realizado são `adherence-today` (variante grande), `adherence-week`, `adherence-geral`. **`adherence-geral` agrega `Object.keys(stats.dayStudyPlanned).filter(k => k <= today)`** — todos os dias passados+hoje, não só mês corrente.

**Novos campos de `computeStats`** (todos populados na mesma passada do loop existente — sem segunda iteração):
- `dayStudyPlanned: { "YYYY-MM-DD": min }` — soma `endTime-time` de todos blocos `type==='estudo'` no dia (planejado).
- `dayStudyDoneMins: { "YYYY-MM-DD": min }` — mesmo somatório, mas só dos blocos checked. Inclui hoje (sem exploit possível — aderência não é XP).
- `dayMetGoal: { "YYYY-MM-DD": bool }` — `dayStudyDoneMins[key] >= dailyStudyMin`.
- `cycleStats: { N: {done, total} }` — só dias fechados (`isPast`), só blocos de estudo, agrupados por `b.cycle`.

**Importante**: `dayStudyMins` (streak/bônus) e `stats.coins`/`stats.studyMins` (totais) agora usam **duração real** (`endTime-time`) — não mais `cfg.pomo` fixo. Eventos checked contam junto com estudo. Mini-estudos contam pela duração real (não inflados pra pomo cheio). Helpers: `monthKey(d)` (`domain/time.ts`), `currentWeekKeys(now)` e `adherence(...)` (`domain/analytics.ts`).

## Sistema de gamificação

Valores e thresholds estão definidos no código (`LEVELS`, `calcXP`, etc.). Aqui só o conceito:

- **XP**: ganho por bloco concluído. Fórmula simples: `calcXP(minutes) = minutes * 2`. Estudo de 25min → 50 XP; estudo de 60min → 120 XP. Pausa usa `bxp = max(1, breakDur)` (~1 XP por minuto), mantendo a proporção estudo:pausa de 2:1. Evento checked também vale `calcXP(duração)` — pra eventos contarem como aulas/compromissos legítimos que rendem progresso.
- **XP negativo**: só existe no modo hardcore (desistir de um estudo = −2× o XP do bloco, pro usuário e pro pet, na hora). Ver "Modo hardcore".
- **Níveis**: lista ordenada definida em `LEVELS` com thresholds e nomes. Calibrado pra escala nova (`calcXP=min*2`): 0/250/750/1500/2500/4000/6000/10000.
- **Moedas**: duas fontes. (a) Por bloco de estudo OU evento concluído: **1 moeda por minuto** (`coinsForBlock(b, dur) = dur`). Estudo de 25min → 25 moedas; evento de 90min → 90 moedas. Sem multiplicador progressivo — XP é 2× e moeda é 1×, sempre, exceto bônus de skill/streak (que podem somar em cima). (b) Bônus diário por faixa de streak: `DAILY_BONUS_TIERS` (5/8/12/18/25 moedas nos marcos 1/3/7/14/30 dias). O bônus só conta no dia se total de estudo concluído ≥ `config.dailyStudyMin`. Saldo = total − `coinsSpent`.
- **Streak**: dia entra no streak só se `dayStudyMins ≥ config.dailyStudyMin` (padrão 60min, editável em Configurações → Geral). `dayStudyMins` soma duração real de **estudos e eventos** checked — pausas não contam. Mudar o mínimo recalcula retroativamente.
- **Sons**: Web Audio API (sem arquivos externos), diferentes por tipo: `check`, `estudo`/`pausa_curta`/`pausa_longa` (fim do bloco fora do foco) e `sucesso` (fim do bloco dentro do foco — arpejo "deu certo").
- **Notificações**: Web Notifications API quando timer acaba.

## Sistema de pets

**Modelo**: espécie ≠ forma ≠ instância.

- **Espécie** (`PETS`) é o que a loja vende: preço, forma base, caminhos de evolução, sugestões de nome.
- **Forma** (`FORMS`) é o que aparece na tela: nome, emoji, sprite, skills possíveis. Cachorro, Pastor alemão e Lobo são três formas; só Cachorro é espécie. Espécies hoje: Cachorro, Gato, Cobra, Vaca, Pomba (todas com sprite e pelo menos uma skill), cada uma com 4 formas de evolução — 25 formas ao todo.
- **Espécie renomeada** passa por `SPECIES_RENAMES` (`owl` → `dove`: a coruja virou pomba em 2026-09-03). `normalizePetInstance` traduz na leitura e desliga skill que a forma atual não tem; o **id da instância continua o antigo** (`owl`), então checks salvos seguem creditando.
- **Instância** (`PetInstance`, em `state.pets.owned`) é o pet adotado: `{ id, species, name, xp, path, stage, skill, skillActivatedAt, adoptedAt }`. Dá pra ter dois cachorros — cada um com nome e XP próprios. O id é o da espécie se estiver livre (`dog`), senão `dog-2`, `dog-3`. `state.pets.active` é o id da instância; `activeSince` marca quando foi equipada.

Adicionar um pet novo:

1. Colocar sprites em `public/idle/pets/{form}/` (`0.png` a `{frames-1}.png`, 32×32 com transparência — `scripts/pixel-sprites.mjs` é o padrão)
2. Adicionar a forma em `FORMS` e a espécie em `PETS`, em `src/domain/pets.ts`. Se a imagem do sprite falhar (pasta não existe ainda), o emoji entra no lugar — dá pra cadastrar antes da arte existir.

**Skill da base já vem ligada** (2026-09-10): `newPetInstance` liga a skill quando a forma tem **uma só** — que é o caso de toda forma base. O pet é escolhido pela skill (o onboarding e a loja mostram "Fiel. Comemora o seu primeiro estudo do dia."), e ela vinha `null`, atrás de um toggle que nem o tour nem o card do pet mencionam: quem escolheu pela skill não ganhava nada. Escolher entre duas ou três continua sendo do usuário — só a que não tem escolha já vem ligada.

**Nome**: obrigatório ao adotar, mas o campo já vem preenchido com uma sugestão sorteada de `species.names` (🎲 sorteia outra). 1–16 caracteres (`normalizePetName`). Renomear é grátis (✏️ no card em "Meus pets"). O nome aparece no card "Pet ativo", em "Meus pets", no resumo do fim do dia e nos toasts.

**Nível do pet**: curva própria, separada da do usuário — o próximo nível custa `50 + 20·(nível−1)` XP (`petXpToNext`/`petLevelStart`/`petLevelFromXP`). Lv. 2 = 1 pomo, Lv. 10 ≈ 10h, Lv. 30 ≈ 80h. Sem teto.

**Evolução**: a espécie declara `paths` (caminhos), cada um com `stages: [{ level, form }]`. `evolutionOf(pet)` diz o que está disponível: `choose` (chegou no nível e ainda não tem caminho — escolhe entre as opções), `advance` (próximo estágio do caminho), `locked` (falta nível) ou `null` (não evolui / fim do caminho). `evolve` devolve a instância nova: nome, XP e nível continuam; só `path`/`stage` mudam (a forma é derivada por `petForm`); skill que a forma nova não tem é desligada. **Definitivo.** **Toda espécie tem dois caminhos, cada um com dois estágios** (`EVOLVE_LEVELS = [5, 15]`, montados por `twoStages`): a **escolha** no **Lv. 5** (320 XP ≈ 2h40 de estudo com o pet equipado — cedo o bastante pra dar senso de evolução) e o **avanço** no **Lv. 15** (2520 XP ≈ 21h — umas três semanas com o mesmo pet; IDEIAS.md falava em Lv. 30, e 15 é a metade pela mesma razão que a escolha desceu de 10 pra 5). A árvore (protótipo de 2026-09-06):

| Espécie | Caminho | Lv. 5 | Lv. 15 |
|---|---|---|---|
| Cachorro | Companheiro | Pastor alemão | Cão lendário (bandana, medalha, grisalho) |
| Cachorro | Selvagem | Lobo | Lobo lunar (prateado, lua na testa) |
| Gato | Sábio | Gato egípcio | Esfinge (nemes) |
| Gato | Selvagem | Lince | Tigre |
| Cobra | Ancestral | Naja | Basilisco (coroa) |
| Cobra | Mítico | Serpe (chifres, brasa) | Dragão (asa, fogo) |
| Vaca | Campeã | Vaca premiada (flor, sino) | Vaca dourada |
| Vaca | Selvagem | Touro | Bisão |
| Pomba | Solar | Pássaro de fogo | Fênix |
| Pomba | Rapina | Falcão | Águia |

Regra das skills ao longo do caminho — **a escada é 1 → 2 → 3**: a espécie nasce com **uma** skill, a **escolha** do Lv. 5 tem **duas** e o **avanço** do Lv. 15 tem **três**. A escolha pode **trocar** a da base (o lobo larga a Fiel — o selvagem é transformação); o avanço **nunca tira**, só acrescenta. **Nenhuma forma repete o conjunto de skills de outra**: escolher caminho é escolher um jeito de estudar, não um sprite. Os três são teste (`tests/pets.test.ts`). Ver a tabela em "Sistema de skills" e o [bestiário](docs/bestiario.html). UI: botão "✨ Evoluir" no card em "Meus pets" abre `#pet-evolve-panel` com um card por caminho (sprite, nome da forma, descrição, skills, e a linha `.evo-path-next` "depois: Tigre · Lv. 15" com o sprite do segundo estágio — `EvolutionOption.next`); no Lv. 15 o mesmo modal mostra só o próximo estágio (`advance`); "Evolui no Lv. N" enquanto trancado.

A **loja de pets** vive num modal próprio (`#pets-shop-panel`), aberto pelo botão "🛒 Loja de pets" no perfil. Grid de 2 colunas, card vertical (imagem/emoji + nome + preço + botão). Os cards são `ShopPetCard` (espécie, com preço) e `OwnedPetCard` (instância, em "Meus pets").

**Estrutura da área de pets no perfil** (abaixo dos stats 4-col):

1. **Card "Pet ativo"** (`.active-pet-card`, id `#active-pet-card`): destaque do pet equipado no momento. O card é **cena + corpo** (`flex-direction:column`, `padding:0`, `overflow:hidden`): em cima o **habitat**, a casa do pet (`.habitat[data-habitat="campo"]`, `aria-hidden` — céu, sol, dois morros, árvore e grama em CSS puro com os tokens `--hb-*`; `data-habitat` é o gancho pra outros cenários, e hoje só "campo" existe), com o sprite da forma atual parado no meio dela (`#ap-sprite`, 54×54 no celular e 96×96 a partir de 1100px); embaixo o `.ap-body`, com a tag "Pet ativo" + **nome do pet** + badge `Lv. N` + nome da forma (`#ap-species`) + barra de XP + texto `X / Y XP · faltam Z pro Lv. N+1`. Quando não tem pet equipado, esconde e mostra `#no-active-pet` ("Nenhum pet equipado. Adote um na loja e equipe em 🐾 Meus pets."). Populado por `renderActivePetCard()` chamado dentro de `renderProfile()` (que já roda em equip/compra). Cálculo usa `petProgress` (curva própria do pet). **O card é um botão** (`role=button`, Enter/Espaço, chevron `›`): tocar abre o modal do pet (`#pet-detail-panel`) — o mesmo `OwnedPetCard` de "Meus pets" sozinho (equipar, ✏️ renomear, ✨ evoluir, skills) mais "Ver todos os pets ›" (`#pet-detail-all`, troca pelo "Meus pets"). Renomear/evoluir abertos dali voltam pra ele ao fechar (`ProfileModal.back`). **Aviso de evolução**: quando o pet equipado pode evoluir (`canEvolveNow`, derivado de `evolutionOf` na render — nada persistido), o card mostra o selo `#ap-evo-badge` "✨ Pode evoluir" ao lado do nível; fica enquanto a evolução estiver disponível e some sozinho ao evoluir. Convida, não pressiona: sem toast, sem pulsar.

2. **Botão "🐾 Meus pets"** (`.shop-open-btn` com `onclick=openMyPets()`): abre o modal `#my-pets-panel` com a grade completa de todos os pets adquiridos. Mostra contador `X/N ✨` no canto direito (`#my-pets-count`; espécies distintas adotadas / espécies no catálogo), e antes dele, só quando há pet que pode evoluir, `#my-pets-evo` "1 pode evoluir" / "N podem evoluir" (`petsReadyToEvolve`). Modal lista todos via `renderOwnedPets()` populando `#my-pets-grid`, inclusive o ativo (com badge "✓ Equipada"). Equipar/desequipar de dentro do modal é grátis e instantâneo.

3. **Botão "🛒 Loja de pets"**: abre o modal de compra. O card de cada espécie mostra sprite, nome, a **linha de personalidade** (`strings.pets.traits`, a mesma do onboarding), o preço, e **"✓ você já tem N"** quando você já adotou aquela espécie; o cabeçalho do modal traz o **saldo** (`#shop-balance`). Antes eram cinco cards idênticos de 🪙 150 sem base nenhuma pra escolher, e o saldo só existia no Perfil, atrás do modal — dá pra ter dois cachorros, mas não sem saber.

**Subtitle do hero do perfil** (`#char-title-sub`): mostra **só o nível do usuário** (ex: "Dedicado"). Info do pet vive no card dedicado abaixo — sem redundância.

Removido: grade inline `#my-pets-grid-profile` e header "Meus pets" inline. O id `my-pets-count` foi reaproveitado pro contador no botão.

**Pet ganha XP (com fechamento de dia)**: quando o usuário marca um bloco, `toggleCheck` salva o pet equipado **no momento do check** em `state.checks[date][time] = { pet: instanceId | null, bonus }`. XP não é creditado na hora — fica "pendente". `applyPendingPetXP()` processa dias **anteriores a hoje** entre `state.pets.xpProcessedUntil + 1` e ontem: pra cada **estudo ou evento** done (nos blocos do dia **como eles eram** — `blocksForDay`, com almoço e janelas editadas daquele dia), credita `b.xp` no pet salvo naquele check. Roda em `initApp()` e `renderProfile()` (cobre o caso do app ficar aberto atravessando a meia-noite). Idempotente — não credita 2x. Na primeira execução pós-mudança (`xpProcessedUntil == null`), zera o XP de todas as instâncias e marca `yesterday` como processado (sem aplicar retroativamente) — e **não salva** se não creditou nada: numa conta nova, salvar aí criaria o doc antes do onboarding terminar, e um reload pularia o onboarding e o pet inicial. Level usa a curva própria do pet (`petLevelFromXP`). Pausa não conta (só estudo+evento). Pet null no momento do check = ninguém ganha XP.

**XP/moedas do usuário também só refletem dias fechados**: `computeStats()` itera todos os dias, mas só agrega `totalXP/totalChecks/coins/studyMins/hourCounts/weekXP/weekChecks/bestDay/activeWeeks` quando o dia está fechado (`isPast = key !== todayKey || isDayClosed(key)`). Stats específicos de hoje (`todayXP`, `todayCoins`, `estudosToday`, `pausasToday`, `dayStudyMins[today]`) continuam refletindo o dia atual. Streak ainda usa `dayStudyMins` incluindo hoje (você "está em sequência" se atingiu o mínimo), mas o **bônus de moedas** do streak só entra quando o dia fechou. Motivo: evitar exploit de marcar/desmarcar pra ganhar XP/moedas; também garante consistência com o XP do pet.

**Contador de hoje pendente (UI)**: no `xp-card`, a linha "Hoje" (`#today-xp-val`) mostra em laranja "pendente" os ganhos previstos — `Hoje: +X XP · +Y 🪙` — sempre que hoje tem checks e ainda não foi encerrado, num pill com glow e pulse bouncy (`@keyframes pulse-pending`) quando o valor sobe. Quando o dia fecha, vira "✓ Hoje encerrado" em verde (e os totais já abrigam o ganho). `renderTodayPending(stats)` cuida do display. Marcar/desmarcar atualiza o pendente em tempo real sem reabrir o exploit — o XP real só entra no total quando o dia fecha. **O Perfil também conta essa metade** (`#profile-pending`, 2026-09-10): marcar três estudos e ir ver o pet mostrava `XP TOTAL 0 · BLOCOS 0 · ESTUDO 0h · MOEDAS 0` sem dizer por quê — a regra é boa, mas era a única tela que só mostrava o zero.

**Feedback dopamínico no check**: ao marcar um bloco (não ao desmarcar), o click handler em `renderBlocks` dispara três efeitos visuais sincronizados com o som `playSound('check')`: (1) `spawnCheckRipple(rect)` joga um anel verde expandindo a partir do check, (2) `spawnFloatGain(checkEl, xp, coins)` cria um overlay flutuante com "+X XP" verde grande e "+Y 🪙" laranja menor que sobem e somem em ~1.2s, (3) o badge "Hoje" pulsa no próximo `renderXP`. A `rect` do check é capturada **antes** de `toggleCheck`/`renderAll` porque o re-render destrói o nó; os overlays vivem em `document.body` (não dentro da row) pra sobreviver. CSS: `.float-gain`, `.check-ripple`, `@keyframes gain-fly-xp/gain-fly-coin/check-ripple`.

**Encerrar o dia manualmente**: na aba Plano (só pra hoje, só se ainda não fechado), aparece o botão **"✓ Encerrar o dia"** abaixo da lista de blocos. Clicar abre o modal `#finish-day-confirm` com aviso de que é decisão final. Confirmar:
1. Captura `before = snapshotForSummary()` (XP, moedas, level idx, pet XPs antes).
2. Adiciona `state.closedDays[today] = true`.
3. Chama `applyPendingPetXP()` na hora.
4. Re-renderiza.
5. Captura `after = snapshotForSummary()`.
6. Mostra o modal **`#day-summary-panel`** com `renderDaySummary(before, after)`: cards "+X XP" e "+Y Moedas" no topo, banner verde "🆙 Subiu pro nível N" se o usuário upou, e uma linha por pet que ganhou XP (sprite + nome + Lv. antiga → nova ✨ se upou, e "✨ Pode evoluir — quando quiser" se o XP de hoje destravou uma evolução — `PetGain.evolutionUnlocked`, calculado por `daySummary` com as instâncias depois do crédito; só no dia em que destravou, não em todo resumo enquanto ele não evolui).

A partir daí, `computeStats` trata hoje como dia fechado (XP/moedas entram nos totais na mesma hora), `toggleCheck` no-op pra esse dia, e a UI dos blocos fica esmaecida (`.day-closed`) — clicar mostra toast "Dia encerrado 🔒". O botão vira banner cinza "✓ Dia encerrado". Não há reabrir — é definitivo. A virada de meia-noite continua sendo fallback automático.

**Prompt automático no fim do dia**: `scheduleEndOfDayPrompt()` calcula o `endTime` do **último bloco de estudo** do dia (via `lastStudyEndToday()`) e agenda um `setTimeout` único pra disparar nesse momento exato. Sem polling. Quando dispara, `checkEndOfDayPrompt()` verifica as condições (hoje não fechado, ≥1 check hoje) e abre o modal `#day-end-prompt` com 2 opções:
- **"✓ Encerrar o dia"** → fecha o prompt e abre o `#finish-day-confirm` (fluxo normal).
- **"⏰ Prolongar estudos"** → expande inline um input `time` (sugerindo agora+1h) + botão Prolongar. Salvar muda `state.config.end` pro novo horário, chama `clearBlockCache()` e re-renderiza — o dia se estende e o app continua a gerar blocos.

Reagendamento: `scheduleEndOfDayPrompt()` é chamado em `initApp`, em `endPromptSaveExtend` (depois de prolongar — reseta `endPromptShown` pra permitir novo disparo com o novo horário), e em `saveSettings` (config mudou → último bloco pode ter mudado). `confirmFinishDay` faz `clearTimeout` no timeout pendente (higiene — `isDayClosed` já segura, mas evita disparo desnecessário). Flag `endPromptShown` evita reabrir o prompt depois que já apareceu uma vez (até que prolongar/saveSettings reset). Persistência: dia encerrado fica em `state.closedDays` no Firestore, então a checagem `isDayClosed(today)` em `checkEndOfDayPrompt` pula naturalmente em qualquer reabertura do app no mesmo dia.

**Hero scene do perfil**: container com gradient escuro contém personagem (`#char-sprite`) + pet ativo (`#pet-sprite`) lado a lado. Abaixo, o nome, o badge `Lv. N` (`#char-level-badge`) e o subtitle (`#char-title-sub`) com **só o nome do nível** — ver "Subtitle do hero do perfil" acima. Canto direito mostra "próximo nível · X XP". Stats em grid 4-col: XP total / Blocos / Estudo (horas) / Moedas (card dourado destacado).

**Economia real** (não é mais decisão futura): ao adotar um pet, abre modal de confirmação `#pet-buy-confirm` ("Adotar X por 🪙 Y?"). Ao confirmar (com o nome preenchido): cria a instância em `state.pets.owned`, equipa (`active` + `activeSince`), **`state.coinsSpent += species.price`**. Saldo exibido em `#char-coins` = `getCoinBalance() = stats.coins - coinsSpent` (nunca negativo). Se saldo < preço, botão "Adotar" ganha classe `.shop-btn.locked` (opacity .5, cursor:not-allowed) e clicar dispara toast "Moedas insuficientes" — não abre o modal. **Equipar/desequipar é grátis** e instantâneo (sem confirmação, sem custo). Cancelar sessão zera `coinsSpent` junto com o resto.

## Sistema de skills

Skills são um catálogo global (`SKILLS`, em `src/domain/progression.ts`): `{ id, name, desc, tier, rule }`. `desc` guarda **só a condição** ("em estudos que começam a partir das 18h"); o texto que o usuário vê vem de `skillDesc(skill, nível)`, que põe o "+X% XP" na frente. Cada **forma** de pet lista quais ids ela pode ter (`FORMS[form].skills`); a mesma skill pode aparecer em mais de uma forma. **O bestiário (`docs/bestiario.html`) desenha tudo isto** — ver "Bestiário" abaixo.

**O equilíbrio é o ponto (rebalanceado em 2026-09-09).** A regra que rege o catálogo:

> Uma skill vale o mesmo por dia pro estudante cuja rotina ela combina. O que muda entre elas é *quando* acontece, não *quanto* vale.

Cada skill declara um **tier**, e o tier declara `share` (a fatia do XP de um dia típico que ela encosta, referência: 8 estudos de 25 min) e `weight` (o multiplicador do bônus). O produto é o mesmo nos três — `SKILL_DAY_SHARE = 0.375`, **teste garante**:

| Tier | Acontece | `share` | `weight` | Lv. 1 | Teto (Lv. 11) |
|---|---|---|---|---|---|
| `alta` | ~3 de 8 estudos | 0,375 | ×1 | +5% | +15% |
| `media` | ~1,5 de 8 | 0,1875 | ×2 | +10% | +30% |
| `baixa` | 1 de 8, sempre | 0,125 | ×3 | +15% | +45% |

Na prática toda skill rende ~2% do XP do dia no Lv. 1 e ~6% no teto — reconhecimento, nunca obrigação. Isso também conserta sem regra nova o bug em que **Lua cheia era estritamente pior que Noturno** (mesmo bônus, faixa mais estreita): agora a mais estreita pesa o dobro, então às 19h a Noturno paga e às 22h a Lua cheia paga mais. Um teste cobra isso pra qualquer par de faixas na mesma forma. O que o princípio **não** promete, de propósito: se a rotina não combina, a skill não acontece (madrugador com Noturno ganha zero) — escolher é o jogo.

**As 17, por tier:**

- **`alta` (acompanha o dia)** — `madrugador` (antes das 9h), `vespertino` (12h–18h), `noturno` (a partir das 18h), `maratona` (do 5º estudo do dia em diante), `recomeco` (**todo** estudo do dia em que se volta depois de um dia que contava e ficou em branco), `descansado` (todo estudo do dia seguinte a uma folga), `hora-extra` (estudo num dia de folga — o dia bônus), `afinco` (estudo dentro de um grupo de estudo).
- **`media` (acontece às vezes)** — `lua-cheia` (a partir das 21h), `preguica` (estudo logo depois de uma pausa longa), `aula` (evento que conta como estudo).
- **`baixa` (uma vez por dia)** — `fiel` (o primeiro estudo **marcado**), `ponto-final` (o último estudo **do plano**), `rumina` (logo depois de uma refeição: intervalo de `MEAL_MIN_MINS` = 30 min ou mais), `retomada` (logo depois de um evento — a volta da aula), `constancia` (o estudo que bate a meta do dia), `empenho` (o estudo que fecha um grupo).

`recomeco` e `descansado` andam juntas de propósito: `previousCountingDay` (em `domain/checks.ts`) pula as folgas exatamente como `allDays()`, então **folga não é sumiço** — quem descansou de propósito cai na Descansado, quem sumiu cai no Recomeço. `recomeco` é o anti-streak do app: paga por voltar, nunca cobra por faltar.

**Quem tem o quê** (a base, e o que cada estágio acrescenta): Cachorro → fiel · Pastor alemão +retomada · Cão lendário +constancia · Lobo → noturno, maratona · Lobo lunar +lua-cheia · Gato → preguica · Gato egípcio +afinco · Esfinge +empenho · Lince +noturno · Tigre +hora-extra · Cobra → constancia · Naja +rumina · Basilisco +ponto-final · Serpe → lua-cheia, maratona · Dragão +descansado · Vaca → rumina · Vaca premiada +descansado · Vaca dourada +fiel · Touro → madrugador, hora-extra · Bisão +maratona · Pomba → aula · Pássaro de fogo +recomeco · Fênix +descansado · Falcão → madrugador, vespertino · Águia +ponto-final. Toda skill do catálogo mora em pelo menos uma forma, e toda forma tem um conjunto único (testes garantem os dois).

**Uma skill ativa por pet**, guardada na instância (`pet.skill`); `pet.skillActivatedAt` marca a troca. Clicar na ativa desliga; clicar em outra troca; `toggleSkill` recusa skill que a forma não tem. Evoluir pra uma forma que não tem a skill ativa desliga ela — é também o que cobre uma reatribuição de catálogo (a Pomba perdeu a `madrugador` no rebalanceamento; um pet com ela ativa tem ela desligada na leitura por `normalizePetInstance`, sem migração).

**Anti-exploit (decidido no check, não retroativo)**: `skillEligible(b, dateKey, ctx)` exige `dateKey === hoje` e a skill ativa **desde antes do bloco começar** — `ctx.activatedAt = max(pet.skillActivatedAt, state.pets.activeSince)`, então equipar o pet no fim do bloco também não vale. Depois vem a regra da skill: `hour-range` (as quatro faixas de horário são uma regra só), `first-study`/`nth-study` com `ctx.studiesCheckedToday`, `last-study` com `ctx.isLastStudy`, `event`, `after` (`long-break`/`meal`/`event`) com `ctx.prevBlock`, `meets-goal` com `ctx.studyMinsToday` + `ctx.dailyStudyMin`, `in-group`/`completes-group`, `comeback`, `after-rest` e `bonus-day`. **Nenhuma skill vale pra pausa** (teste varre as 17). `application/checks.ts` monta o contexto **só do que o plano do dia já sabe** — grupos por `groupOf`/`blockInGroup`, folga por `isRestDayKey`, dia bônus por `isBonusDayKey`, o dia da volta por `previousCountingDay`. Nada disso é campo novo no documento salvo.

Se elegível, `toggleBlockCheck` grava no check o bônus **do nível do pet vezes o peso do tier** (`skillBonus` = `skillBonusForLevel` × `SKILL_TIERS[tier].weight`; `skillBonusForLevel` é 5% no Lv. 1, +1% por nível, teto de 15% no Lv. 11 — `SKILL_BONUS_BASE`/`SKILL_BONUS_PER_LEVEL`/`SKILL_BONUS_MAX`); senão `bonus: 0`. O nível é o do XP **já creditado** (`petLevel(pet)`): o XP de hoje só entra ao fechar o dia, então durante o dia o nível fica "atrasado" — de propósito, é o que impede marcar/desmarcar pra subir o bônus no meio do dia (teste cobre). Bônus salvo é permanente — desligar a skill depois não revoga, e checks antigos continuam com o número deles (sem migração).

**XP efetivo**: `xpFromCheck(b, check)` retorna `Math.round(b.xp * (1 + (check.bonus || 0)))`. Usado em `computeStats` (todos os pontos onde XP é agregado), em `computePendingPetXP` (o pet também recebe com bônus) e no feedback flutuante do check (mostra o número final, não o base).

**UI**: dentro do card do pet no modal `#my-pets-panel`, abaixo de Equipar/Evoluir, aparece a seção "Skills" com toggles estilo switch. Estado visual: `.pet-skill-row.active` = borda verde + nome em accent + switch `.ps-toggle.on` (knob deslocado). Toggle é `<button>` (acessível por teclado). A tela lê `SKILLS` e `skillDesc`, então skill nova aparece sozinha.

**Pra adicionar nova skill**: (1) responda primeiro **quantas vezes ela acontece num dia de 8 estudos, pra quem ela combina** — a resposta é o `tier`; (2) entrada em `SKILLS` com `tier` e `rule` — se a regra é inédita, um `kind` novo em `SkillRule`, o caso em `skillEligible` e o campo em `SkillContext` (que `application/checks.ts` monta, só do plano do dia); (3) o id na lista `skills` de pelo menos uma forma, senão o teste reclama. O resto (gravação no check, `xpFromCheck`, render no card, save/load) já cobre.

**O que ficou de fora, e por quê**: skill que paga em **moedas** (outro eixo, precisa de campo novo no check, e "mesma força" fica mole comparando moeda com XP); skill por **sequência de dias** (pagaria por streak e transformaria falhar em perda); skill por **concluir no modo foco** (puniria quem estuda longe do computador e marca depois); **"Imersão"** para pomos ≥ 45 min (quem configura pomo longo ganharia em 100% dos blocos — a pegadinha do "quem não pegou tá perdendo"). Detalhes em `plans/2026-09-09_0120_skills-e-bestiario.md`.

## Bestiário

`docs/bestiario.html` — uma página só com o elenco inteiro: as árvores de evolução com os sprites animados, as 17 skills por tier, o gráfico do equilíbrio, um editor e as instruções de manutenção. Gerado por:

```
node --import ./scripts/ts-loader.mjs scripts/bestiary.mjs
```

Três coisas fazem ele servir a longo prazo: **não guarda dados** (lê `FORMS`/`PETS`/`SKILLS` direto do TypeScript, então não existe versão dele que discorde do app); **os sprites vão embutidos em base64** (108 KB, um arquivo só, abre offline com dois cliques); e **dá pra editar e sair com código** — trocar as skills de qualquer forma na página, ver ao vivo as mesmas regras que os testes cobram, e copiar o bloco `FORMS` pronto pra colar em `pets.ts`. O ciclo é desenhar → colar → `npm test` → regerar.

O gráfico do equilíbrio é uma prova, não uma ilustração: três retângulos (largura = quantas vezes acontece, altura = quanto paga) com a mesma área, e os cantos caindo na curva `x·y = 0,375`.

## O personagem

O boneco que aparece no Perfil e no quarto da coluna do laptop. Desde 2026-09-09 ele é **desenhado na hora**, não é arquivo: `src/domain/avatar.ts` guarda a arte como um **grid de letras** (24×42, 4 frames de respiração), e cada letra é um *papel* de cor — `S`/`s` pele e sombra, `K` o traço, `H`/`h` cabelo e sombra, `L` o cílio, `W`/`E` branco do olho e pupila, `C`/`c` camiseta, `P`/`p` calça, `B` sapato. Quem pinta é `avatarPalette(cfg)`, montada do tom de pele e da cor de cabelo escolhidos. Por isso **6 tons × 9 cores × 6 penteados × 2 corpos = 648 aparências sem um PNG a mais**.

- **A boca sorri** (2026-09-10): os cantos ficam onde a linha reta estava (`K` em y17) e o meio desce um pixel (y18). Era uma barra de 4 px na horizontal, que a 48 px lia como boca fechada e séria — o app é sobre estudar com companhia, não sobre resignação. Testadas cinco curvas (reta, sorriso, sorriso alto, largo, com covinha); o Tomi escolheu a de meio baixo, que abre mais o sorriso.
- **A maçã do rosto é um pixel só** (2026-09-10), em y15, na altura do nariz. Eram dois (y14–y15) desde o desenho original, quando o olho ia só até x8; com o cílio o olho passou a ocupar x7, e a sombra ficou exatamente embaixo do canto de fora — lia como lágrima escorrendo, não como bochecha. Lição pra quem mexer no rosto: **o que fica embaixo do olho depende de onde o olho termina**.
- **O olho tem três camadas** (2026-09-10): o **cílio** (`L`, a pálpebra de cima — o tom de pele escurecido, nunca preto) por cima do **branco** (`W`), com a **pupila** (`E`) de 1 px no meio dele. Antes eram dois blocos chapados (branco em cima, escuro embaixo), que liam como olho semicerrado. A escolha saiu de comparar seis tratamentos lado a lado (bloco, cílio, íris, brilho, amendoado, traço) — o material está em `docs/personagens.html`, gerado por `scripts/avatar-variants-page.mjs`. Íris colorida ficou de fora por ora: numa pupila de 1 px a cor não aparece.
- **Dois corpos** (`BODIES`, 2026-09-10): `reto` (ombro e quadril na mesma largura, pernas afastadas) e `curvo` (ombro 6 px, cintura recortada em 4, quadril abrindo pra 8, pernas juntas). São **forma, não gênero** — qualquer penteado combina com qualquer corpo, e a roupa é a mesma nos dois. Só o desenho do ombro pra baixo muda (`BODY_DRAW`); `head()` é comum. Teste cobra que o curvo afina na cintura, que o reto não afina, e que os dois dividem a mesma cabeça.

- **O contorno sai sozinho** (`outline`): todo pixel cheio que faz fronteira com o vazio vira o tom escuro *daquele material*. É o que faz o cabelo ter contorno de cabelo e a pele contorno de pele — contorno preto chapado achata tom escuro. O traço (`K`) é sempre uma versão bem escura do próprio tom de pele, nunca preto (teste garante: `line` mais escuro que `shade`, `shade` mais escuro que `base`).
- **A escada de camadas** é cabelo de trás → corpo → cabelo da frente, então o Longo cai atrás dos ombros e o Cacheado passa das orelhas.
- **Penteados** (`HAIR_STYLES`): Curto, Ondulado, Cacheado, Longo, Coque, Raspado. Cada um desenha só a metade esquerda e é espelhado (`band`/`pair`). Teste garante que nenhum é igual a outro e que nenhum tapa os olhos ou o rosto.
- **Onde vive**: `state.avatar` (`{ skin, hair, style, body }`) — **fora de `config`**, de propósito. É identidade, não regra do dia: "Cancelar sessão" não mexe nela (como `tutorialSeen`). Campo novo no doc, com default em `hydrateUserDoc` via `normalizeAvatar` (id que sumir do catálogo cai no padrão em vez de virar buraco na tela — e doc antigo, sem `body`, abre no corpo padrão); sem `schemaVersion` novo.
- **Quem pinta**: `src/infrastructure/avatar/sprite.ts` (canvas → `data:` URI, memoizado por aparência; sem DOM devolve string vazia, então teste e SSR não quebram). A UI pede por `application/avatar.ts` (`currentAvatarSprites`, `setAvatar`) — componente não fala com o canvas.
- **UI**: Configurações → Geral → **"Seu personagem"** (`features/settings/AvatarPicker.tsx`, `#avatar-picker`), acima de "Aparência do app" — e o **primeiro passo do onboarding** (`#onb-avatar`, preview `#onb-avatar-preview`), com os mesmos controles (`features/avatar/AvatarControls.tsx`). Preview animado (`#avatar-preview`), swatches de pele (`[data-swatch]`) e de cabelo, pills de penteado (`[data-style]`) e de corpo (`[data-body]`). Aplica **na hora**, sem rascunho nem "Salvar" — trocar de tom de pele sem ver o resultado no mesmo segundo não faz sentido; `setAvatar` já agenda o save. As cores dos botões são inline porque vêm do catálogo: são **conteúdo**, como o sprite de um pet. A regra "cor é token" continua valendo pro CSS.
- **Os PNGs em `public/idle/user/`** não são mais o que o app mostra: existem pra quem precisa de arquivo (`scripts/app-icon.mjs`, que gera os ícones do PWA). Gerados por `node --import ./scripts/ts-loader.mjs scripts/character-sprites.mjs` (`--ascii` desenha os penteados no terminal, `--todos` escreve uma pasta por penteado). e2e **37**.
- **Cosmético novo** = uma letra a mais na paleta e um desenho a mais aqui. Roupa, óculos e acessório são a direção natural; nada disso existe ainda.

## Schema do Firestore

```
users/{uid} {
  schemaVersion, // 4 = a lista de sites saiu do hardcore e virou `config.siteBlock` (2026-09-08). 3 = o almoço virou série de evento (2026-09-06). 2 = pets como instâncias. 1 = Fase 4 (pets por espécie). Ausente = anterior à migração. hydrateUserDoc lê todos
  checks,        // { "YYYY-MM-DD": { "HH:MM": { pet: instanceId|null, bonus: number } } }   pet = id da instância equipada no check; bonus = multiplicador aditivo de XP salvo no check (0, ou 0.05–0.15 pelo nível do pet no momento do check)
  events,        // { "YYYY-MM-DD": [{name, start, end, countsAsStudy, externalId?}] } — eventos avulsos por dia. countsAsStudy: true=tipo 'event' (XP), false=tipo 'intervalo' (só bloqueia). externalId = `ics:<UID>` quando veio de um calendário importado; ausente = digitado à mão, e nenhuma importação encosta nele
  eventSeries,   // [{id, name, start, end, weekdays[], freq, anchor, until, exceptions[], countsAsStudy, externalId?}] — séries recorrentes. A refeição de todo dia é a série `ser_almoco` (sem XP, sem âncora)
  // lunchOverrides existia até o v2 — virou exceção na série + avulso, na leitura
  closedDays: { "YYYY-MM-DD": true, ... },   // dias encerrados manualmente via botão "Encerrar o dia"
  pets: {
    owned: [{                              // v2: instâncias (v0/v1 eram ids de espécie — hydrateUserDoc migra)
      id, species, name, xp,               // id = espécie se livre ("dog"), senão "dog-2"; xp creditado quando o dia do check fecha
      path, stage,                         // caminho de evolução escolhido (null = ainda não) e estágios aplicados (0 = forma base)
      skill, skillActivatedAt,             // skill ativa (uma por pet) e ms da última troca
      adoptedAt                            // ms (0 em pets migrados)
    }, ...],
    active: instanceId | null,
    activeSince: number,                   // ms de quando o ativo foi equipado (anti-exploit do bônus)
    xpProcessedUntil: "YYYY-MM-DD" | null  // último dia já processado por applyPendingPetXP (null = ainda não inicializado)
  },
  // skills: { owl, activatedAt } existia até o v1 — migrado pra dentro da instância
  coinsSpent,
  groups,        // { "YYYY-MM-DD": [{id, start, end, name, goal}] } — grupos de estudo. Anotação por horário; nunca entra no gerador (ver "Grupos de estudo")
  ageConfirmed,  // boolean — a pessoa declarou ter 16 anos ou mais ao criar a conta. Só o booleano: nunca a idade nem a data de nascimento. Ausente/false = nunca foi perguntado (doc anterior a 2026-09-11). Apagar o histórico NÃO zera: a declaração é sobre a pessoa, e a caixa do onboarding reaparece marcada
  safetyNet,     // { at, doc } | null — o estado de antes de "Apagar todo o histórico", pra dar marcha a ré por 30 dias (ver "Meus dados"). A foto nunca contém outra foto dentro, fica de fora do arquivo baixado, e é descartada no primeiro acesso depois do prazo. Ausente = null
  tutorialSeen,  // { plan?: true, profile?: true, analytics?: true } — áreas cujo tour contextual já foi visto. Ausente = tour ainda não visto (conta antiga vê uma vez). Cancelar sessão não zera
  windowOverrides, // { "YYYY-MM-DD": { studyWindows: [{start, end}] } } — janelas só daquele dia; lista vazia = dia livre (ver "Janelas do dia")
  penalties,     // { "YYYY-MM-DD": [{ time, endTime, name, xp, pet, petXp, at, reason }] } — desistências do modo hardcore. xp/petXp = o que de fato saiu (limitado ao saldo); reason = 'quit' | 'abandon'. Bloco com registro está abandonado (sem check, trancado). Ausente = {}. Cancelar sessão zera
  notifications, // [{ id, kind, at, read, data }] — o sininho (ver "Notificações"). `id` É a chave de dedup e deriva do acontecimento, nunca do relógio; `data` guarda os números (o texto mora em strings.ts). Mais nova primeiro, teto de 40. Ausente = []. Cancelar sessão zera
  pauses,        // { "YYYY-MM-DD": [{ at: "HH:MM", mins }] } — pausas do timer, em ordem de `at`. O gerador estica o estudo/pausa que contém `at` em `mins` e empurra o resto do dia (ver "Pausar o bloco"). Ausente = {}. Só nasce ao retomar (a pausa aberta fica no localStorage). Cancelar sessão zera
  meta: { writer, writtenAt },  // carimbo de quem salvou por último: writer = id desta carga da página (CLIENT_ID), writtenAt = ms do relógio de quem escreveu. Só pra reconhecer eco/repetição no sync — nunca entra no estado
  config: {
    studyWindows,   // [{start:"HH:MM", end:"HH:MM"}, ...] — janelas de estudo do dia (fonte da verdade)
    start, end,     // derivados de studyWindows (primeira/última) — mantidos só pra retrocompat
    // lunch, lunchDur, hasLunch existiam até o v2 — o almoço é um evento (série `ser_almoco`)
    pomo, shortBreak, longBreak,
    periodStart,    // "YYYY-MM-DD" | null (null = "sempre", sem fim)
    periodEnd,      // "YYYY-MM-DD" | null
    skipWeekends,   // boolean (true = sáb/dom sem blocos nem stats)
    dailyStudyMin,  // number (15–240, padrão 60). Min de estudo/dia pra contar streak + bônus.
    hardcore,       // { enabled } — modo hardcore, só a penalidade (ver "Modo hardcore"). Ausente = desligado
    siteBlock       // { enabled, mode: 'blacklist' | 'whitelist', sites: ["chess.com", ...] } — bloqueio de sites (ver "Bloqueio de sites"). Ausente = desligado; sites sempre normalizados (só domínio, sem apelido — a expansão é na hora de publicar). Até o v3 isso morava dentro do `hardcore`; `hydrateUserDoc` migra
  }
}
```

Schema flat funciona pro volume atual. Quando ficar lento, considerar subcollection por dia.

`periodStart`/`periodEnd` controlam o range de `WEEKS` em `buildWeeks()` (`domain/weeks.ts`). `skipWeekends` é respeitado em `forEachDay()` e `blocksForDay()`. Doc inexistente em `users/{uid}` = novo usuário → modal de onboarding aparece (ver "Onboarding" abaixo).

## Os documentos legais

Política de privacidade, termos de uso e Impressum vivem em `public/legal/`, como **HTML estático fora do app**. São três razões, e nenhuma é estética: o console do Google pede uma URL pública de política de privacidade pra publicar a tela de consentimento (sem isso o login trava no usuário 101), o Impressum do § 5 DDG tem que ser alcançável sem login, e nenhum dos três pode depender de o app carregar — se o Study Pets estiver fora do ar, o dever continua de pé.

- **O service worker não pode engoli-los.** `navigateFallback` manda qualquer navegação pro `index.html` precacheado; o `navigateFallbackDenylist: [/^\/legal\//]` em `vite.config.ts` é o que faz `/legal/privacidade.html` abrir a página e não o app.
- **As fontes são as mesmas do app, servidas do mesmo lugar.** O `legal.css` redeclara os `@font-face` apontando pra `/fonts/` porque as páginas não passam pelo bundle do Vite. Uma política de privacidade que manda o IP do leitor pro CDN do Google seria piada — e `tests/legal.test.ts` cobra isso.
- **Os campos que só o responsável pode preencher ficam visíveis** (`<span class="falta">`), e enquanto existir um deles a página mostra o aviso `.rascunho` no topo. O teste amarra os dois: com campo faltando, o aviso é obrigatório; sem campo faltando, ele tem que sair. É o que impede tanto publicar com endereço inventado quanto deixar tarja vermelha pra sempre. **Faltam três: endereço postal, e-mail de contato e a região do Firestore** (esta última se confere no console do Firebase).
- **Dois documentos internos acompanham os três públicos**: `docs/registro-tratamento.md` (o art. 30 — que dados, por quê, com quem, por quanto tempo, e as medidas do art. 32) e `docs/plano-de-incidente.md` (o que fazer nas 72 horas do art. 33, com modelo de notificação pronto). Não vão pro ar; são o que se mostra a uma autoridade. Existem porque nenhum dos dois se escreve na hora que precisa. O teste cobra que interno e público estejam no **mesmo estado de preenchimento** — preencher o endereço nas páginas e esquecer dos documentos é a armadilha real.
- Onde aparecem: rodapé da tela de login (`.ls-legal`, a única página que qualquer pessoa vê sem conta) e Configurações → Geral → "Documentos" (`#legal-privacidade`, `#legal-termos`, `#legal-impressum`), logo antes da Zona de perigo — quem procura "o que vocês sabem sobre mim" e quem procura "como apago isso" olham pro mesmo canto.
- **Idade mínima: 16 anos**, a escolha alemã no art. 8 do GDPR. O último passo do onboarding tem a caixa `#onb-age`, que destrava o `#onb-begin`; `finishOnboarding` recusa com `age-unconfirmed` antes de mudar qualquer coisa. O que fica salvo é `ageConfirmed: true` — o registro de que a pergunta foi feita e respondida, **nunca a idade nem a data de nascimento**. Documento anterior à pergunta abre `false`, que é literalmente "nunca foi perguntado"; conta que já existe não é interrogada de novo, porque o onboarding só abre pra conta nova. **Apagar o histórico não zera a declaração** — ela é sobre a pessoa, não sobre o histórico, como `avatar` e `tutorialSeen` —, e a caixa reaparece já marcada.

## Visual

- **Café de casa (claro) é o tema padrão** desde 2026-09-07; o escuro original é o secundário (Configurações → Geral → Aparência, ou `?tema=escuro`)
- Cor primária verde lima (definida em CSS variable)
- Fontes: DM Sans (UI), DM Mono (números), Lora (títulos do tema Café de casa), Press Start 2P (título do login no tema escuro). Auto-hospedadas em `public/fonts/` sob a SIL OFL (a licença viaja junto, em `public/fonts/OFL.txt`), só latin e latin-ext. `tests/fonts.test.ts` cobra que todo `@font-face` aponte pra arquivo existente, que nenhum CSS da árvore volte a falar com `fonts.googleapis`/`gstatic`, e que bytes iguais não virem arquivos diferentes
- Layout mobile-first, max-width 480px centralizado; a partir de 1100px vira o grid de três colunas (ver "Tela grande")
- Sprites com `image-rendering: pixelated`. Pets são PNG em `public/idle/pets/`; o **personagem** é desenhado no canvas a cada aparência (ver "O personagem")
- **Modais**: todos centralizados na tela (classe `.panel-overlay.center`). Não usar sheet de baixo pra cima. Exceção: **Configurações não é modal** — é página inteira (ver "Settings é página inteira" acima).

## Direções futuras

- Mais pets (estrutura pronta)
- Chatbot pra otimizar configuração (precisa avaliar custo de API)
- ~~Sync mais responsivo entre dispositivos~~ → feito por snapshot (ver `application/sync.ts`); o que resta é escrita granular por campo, se a janela do "local vence" um dia doer
- Notificação pelo service worker e push do servidor pro fim do bloco com o celular travado (ver IDEIAS.md)

## Decisões adiadas conscientemente

- ~~Não separar em múltiplos arquivos~~ → o limite foi ultrapassado (o script tinha 2903 linhas dentro de um HTML de 4454). Migração em curso: ver `plans/2026-09-02_1552_migracao-vite-ts.md`
- Não migrar Firestore pra subcollection (reavaliar quando salvar virar lento)

## Sempre

- **Nunca criar uma pasta por teste ou por feature.** Checkout separado (git worktree) só se for indispensável, e aí **sempre na pasta única `study-pets/teste`**, reaproveitada por qualquer branch — e **apagada assim que o teste terminar** (`git worktree remove --force teste`). Nem pasta irmã em `Projetos/`, nem `teste-<algo>`. Vale também pra tarefas agendadas
- Atualizar este arquivo quando uma decisão de design mudar
- Testar em `npm run dev:teste` antes de commitar (e `npm test` + `npm run typecheck`)
- Quando notar algo aqui que não bate com o código, perguntar ao usuário antes de "corrigir" — pode ser que o design tenha evoluído de propósito
